# Web/PWA Build Evidence — 2026-08-08

`npm.cmd run mobile-web:build` passed after TypeScript type-checking.

The production bundle contains `index.html`, `manifest.webmanifest`, `sw.js`, a local icon, hashed application CSS/JS, and a hashed QR decoder worker.

The local development server returned HTTP 200 for the application shell. This only proves local server reachability; it does not prove HTTPS, browser service-worker activation, or iPhone behavior.
