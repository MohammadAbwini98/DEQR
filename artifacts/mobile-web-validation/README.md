# Mobile Web/PWA Validation Evidence

This directory contains non-sensitive execution evidence for the active `mobile-web/` receiver. It deliberately excludes transferred private files, device identifiers, Apple credentials, and raw camera frames.

Physical iPhone evidence is **NOT EXECUTED** until a trusted HTTPS origin is available on the device and the user performs the required actions.
