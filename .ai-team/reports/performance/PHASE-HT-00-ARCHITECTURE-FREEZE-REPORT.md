# Phase HT-00 — Architecture Freeze and Baseline — Execution Report

**Status:** `PASS`
**Phase:** HT-00 Architecture Freeze and Baseline (High-Throughput Optical Transfer Program)
**Date:** 2026-08-29
**Branch:** `main` at `c85719a` (`feat(desktop): remediate four transfer UI regressions (DESKTOP-UI-012)`)
**Tree:** clean (`git status --porcelain` empty, `HEAD == origin/main` after push)
**Prior phase:** Large-File Program 13 (physical iOS remediation, `bcccc66` recovery tail) + DESKTOP-UI-012 (`c85719a`)
**Known blockers:** physical iPhone gate (7 gates PENDING, certified max 0 bytes), `physicallyCertified=false` all profiles

## Implemented

- Frozen sender pipeline (file→container, compression sampling 3×256 KiB gzipSync L6, fused hash+measure, systematic-first SegmentEncoder with RobustSoliton, CRC-32 + SHA-256, qrcode byte-mode pinned version/ECC L/quiet 4, integer moduleScale geometry, QrFrameScheduler pull+bounded prefetch 2, effectiveFps) — no code change.
- Frozen receiver pipeline (camera 1280×720 ideal, ROI 86%→720/96, rVFC+500 ms watchdog, canAccept maxInFlight=2, ImageBitmap transferred, jsQR dontInvert, STALE 250 ms, BoundedFingerprintSet 4096 FNV-1a×2, ReceivePipeline v1/v2, SegmentedReceiver maxActiveSegments=2, OPFS sync-handle, Sha256Stream/inflate-verify, export allowlist) — no code change.
- Catalogued every performance-sensitive constant with `file:line` (symbol 239/686/1139/1920, version 10/18/24/32, targetFps 10/12/15/20 + hold 100/83/66/50, segment 489k/1,405k/2,333k/3,932k, repair 1.00/0.75/0.75/0.75, ROI 720, queue 32/2, manifest interval 64, checkpoint 3×24h/810 B@4 GiB).
- Reconfirmed finite-repair-tail risk as **fixed**: `take()` exhausts pass (`done=true` at `streaming-sender.ts:1041`) but `StreamTransferView.tsx:207` auto-`beginRecovery` (`ipc-handlers.ts:196` → `streaming-sender.ts:898`) clears `done` and emits unbounded systematic-first batches via monotonic `recoveryNextSymbolId` (`streaming-sender.ts:1015`); round-robin rejected due to 2-decoder negative progress.
- Established baseline transfer profiles for required tiers via real harnesses (not synthetic guesses): v2 ladder Balanced 0% loss (1 KiB/5 KiB/100 KiB/1 MiB/5 MiB × 3 classes, 15 runs, all `hashMatch:true`) + v1 desktop-pwa-pipeline (5 sizes × 2 loss, 10 scenarios).
- Created `OPTICAL-ARCHITECTURE-BASELINE.md`, `PERFORMANCE-BASELINE.md`, `PROTOCOL-BASELINE.md` in `.ai-team/project-control/` — each cites concrete `src/...:line`.
- Updated `CURRENT-STATE.md` header + inserted HT-00 section.

## Changed Files

- `.ai-team/project-control/OPTICAL-ARCHITECTURE-BASELINE.md` (new, 22,642 B)
- `.ai-team/project-control/PERFORMANCE-BASELINE.md` (new, 13,013 B)
- `.ai-team/project-control/PROTOCOL-BASELINE.md` (new, 13,472 B)
- `.ai-team/project-control/CURRENT-STATE.md` (updated: header 2026-08-29 + HT-00 section after Large-File Phase 13)
- Evidence (gitignored, durable via reports): `.local-run/bench/desktop-pwa-pipeline-ht-phase00-baseline.json`, `.local-run/bench/phase11/phase11-ladder-ht-phase00-baseline.json`, `.local-run/bench/phase11/phase11-ladder-ht-phase00-5mib.json`, `.local-run/bench/qr-frame-roundtrip-ht-phase00-baseline.json`, `PHASE04_CPU` log

No file under `src/`, `mobile-web/src/`, `tests/`, `protocol/` modified — phase is measurement-only by exit criteria.

## Protocol Impact

- **none.** No change to `src/core/protocol.ts` (v1 20-byte header), `src/core/protocol-v2.ts` (manifest 84 fixed / data 32 overhead, u64 sizes, CRC-32), `src/core/segment-encoder.ts`, `src/core/transport-profiles.ts`. Vectors regenerated byte-identical.

## Validation

| Gate | Result | Evidence |
|---|---|---|
| Typecheck (desktop) | PASS | `npm run typecheck` — `tsc --noEmit` exit 0 |
| Typecheck (PWA) | PASS | `npm run mobile-web:typecheck` — exit 0 |
| Unit tests (desktop) | PASS | `npm test` **53 files / 931 tests PASS** (22.3s) — including hostile-stream + 1 GiB synthetic + 4 GiB offset |
| Unit tests (PWA) | PASS | `npm run mobile-web:test` **29 files / 420 tests PASS** (32.3s) — including 1 GiB OPFS write + composition matrix 5 KiB/100 KiB/500 KiB/1 MiB |
| Desktop build | PASS | `npm run build` — `tsc` + `tsc -p tsconfig.main/preload` + `vite build` 116 modules, exit 0 |
| PWA build | PASS | `npm run mobile-web:build` — 49 modules, `receive-worker DbLnCYmf 220 kB`, exit 0 |
| Doctor | PASS | `node scripts/ai/doctor.js` — **PASSED (0 warnings)** |
| Drift | PASS | `node scripts/ai/check-adapter-drift.js` — **Zero drift** |
| Vectors | PASS | `npm run vectors:generate` 15 vectors + `vectors:v2:generate` 24 vectors, `git diff --exit-code` 0 |
| Benchmark (required tiers) | PASS | `desktop-pwa-pipeline --label ht-phase00-baseline` 10 scenarios ok; `phase11-certification --mode ladder --maxMib 5 --tag ht-phase00-baseline` 12 runs ok; `--sizesMib 5 --tag ht-phase00-5mib` 3 runs ok; `qr-frame-roundtrip --label ht-phase00-baseline` 4 scenarios ok; `phase04-qr-profiles --mode cpu` 42 versions ok — all `hashMatch:true` |
| Physical device | BLOCKED — PHYSICAL DEVICE REQUIRED | No camera→screen capture; `physicallyCertified=false` all profiles; matrix `.ai-team/reports/performance/PHASE-11-PHYSICAL-CERTIFICATION-MATRIX.md` 7 gates PENDING |
| `git diff --check` | PASS | exit 0 |
| `git status` | CLEAN + 3 untracked baselines | `HEAD c85719a == origin/main` |

## Before / After Metrics

This phase *is* the before (frozen baseline). After is empty by design — next phase must beat this.

| Metric | Before (this phase, `c85719a`) | After |
|---|---|---|
| Verified goodput 5 KiB (Balanced clean) | **4,096 B/s** (optical 8,152; unique 6.4/s, pipeline 0.006s, held 0.00 MiB, heap 25.6 MiB) | — |
| Verified goodput 100 KiB | **6,400 B/s** (optical 8,507; unique 9.4/s, dup 1.0%, pipeline 0.016s, held 0.10 MiB) | — |
| Verified goodput 1 MiB | **7,864 B/s** (optical 8,507; unique 11.5/s, dup 1.5%, pipeline 0.058s, held 1.00 MiB) | — |
| Verified goodput 5 MiB | **5,041 B/s** (optical 8,507; unique 7.3/s, dup 38%, redundant 38%, repairUsed 0.61, held 1.34 MiB) | — |
| Presented symbols/s (Balanced) | **12.0 configured** (`effectiveFps`), **~11.5-12.0 measured** (`SchedulerStats.effectiveFps`); Turbo 15.0 vs scan ceiling 14.9 (fragile) | — |
| Camera FPS | **capture 25 caps/s theoretical** (40 ms + 12 ms backpressure), **actual ~11-15 decodes/s** jsQR-bound | — |
| Decode FPS | **19.0 ceiling** (v18 L 52.7 ms @720²), 14.9 (v24 L), 20.6 (v10 L) | — |
| Unique symbols/sec | 6.4 (5 KiB) → 11.5 (1 MiB) → 7.3 (5 MiB) | — |
| Duplicate rate | 0% (small) → 38% (5 MiB clean = unconditional 0.75 repair) | — |
| Redundant rate | same as duplicate (no loss) | — |
| Useful overhead (1+r) | 1.75 design / 1.03-1.61 observed (clean, amortized) | — |
| Optical seconds (5 KiB/1 MiB/5 MiB) | 0.0003 / 0.037 / 0.289 | — |
| Verified SHA match | 15/15 `hashMatch:true` (triple-digest: fixture, sender, verified) | — |
| Sender buf / budget | 1.34-1.37 / 1.39 MiB (segment-bounded) | — |
| Receiver held | 0.00-1.34 MiB (flat: 1.34 at 4 GiB prior Phase 11; 1.34 at 5 MiB here) | — |
| v1 in-mem pipeline MiB/s | 5.1 (5 KiB) → 84.6 (1 MiB) — software ~5k× link, not bottleneck | — |

Full JSON: `.local-run/bench/*.json` (gitignored; reports durable).

## Risks / Remaining Issues

- **HSD-01: unconditional repair wastes 43% link.** Every profile emits `repairOverheadRatio` even clean; at 5 MiB 4,642/12,480 frames redundant. Removing on clean worth 1.6× (Phase 11 1.78× at 1 GiB). Recorded, not implemented — needs channel-aware repair (Phase 04/11 of High-Throughput program).
- **HSD-02: presentation vs optical gap not diagnosed in UI until Phase 01.** `SchedulerStats` exists but `StreamTransferView` only polls `StreamingProgressView` every 500 ms.
- **Certified max 0 bytes** — all device gates PENDING; no throughput claim may ship. `Experimental` (v32 1,920 B@20 fps) needs 765 px symbol >720-line capture → not selectable, per design.
- No highly compressible fixture measured this phase — throughput numbers are near incompressible floor as required for records.

## Rollback Notes

- No code to rollback (docs-only phase). Remove `.ai-team/project-control/{OPTICAL-ARCHITECTURE,PERFORMANCE,PROTOCOL}-BASELINE.md` and revert `CURRENT-STATE.md` header + HT-00 section.

## Next Phase Readiness

`READY`

**Reason:** frozen architecture + cite-anchored baselines + 19-field metrics schema + reproducible harness (`desktop-pwa-pipeline`, `phase11-certification ladder`, `qr-frame-roundtrip`, `phase04 --mode cpu` with `--label`/`--tag`) + clean validation gates. Next phase is `PHASE-01-DIAGNOSTICS-AND-BENCHMARK-HARNESS.md` — expose `measured displayed symbols/sec`, `camera actual FPS`, `unique/duplicate/redundant/solved blocks/sec` in diagnostics without changing protocol (per-program milestone M1).
