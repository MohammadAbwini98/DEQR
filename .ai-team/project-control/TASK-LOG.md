# DEQR Task Execution Log

| Date | Task ID | Agent Role | Summary of Changes | Test Verifications Executed | Outcome |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-08-24 | PERF-PHASE-13 | QA / System Architect / Front-end / Back-end | Executed Phase 13 of the Large-File / Maximum-Speed program: corrective remediation after a real iPhone transfer failed with the desktop reaching its end-of-pass screen while the receiver sat in `Receiving transfer` and delivered nothing. **Four confirmed root causes, each proven by a test that fails on the previous code.** (1) `take()` returned `null` for good once the budgeted repair was spent, so a receiver short by one symbol had no possible source for it. (2) `RECEIVING` had no exit meaning "the frames stopped"; the only stall detector watched the video element, which was healthy throughout. (3) Repair-only recovery is impossible - the decoder holds at most `k` pending equations and then rejects everything, measured unrecoverable at 1.5x, 2.5x and 4x overhead - which invalidated the first version of the fix. (4) A failed export wedged the receiver in `EXPORTING`, which did not accept the event its own failure path dispatched. Added `INCOMPLETE` and `RECOVERING` states plus a `STALLED` event driven by time since the last *unique* frame; both new states are absent from `SESSION_CLEARING_STATES`, which is the mechanism preserving partial state. Replaced the sender's terminal behaviour with a systematic-first, per-segment-batched recovery tail (round-robin made negative progress against a two-decoder receiver). Added a v2 targeted resume token carrying run-length gaps, falling back to the 40-character v1 code when the gaps are a single trailing run. Added refusal-by-reason counters, the systematic/repair split and `usefulThroughput()`; worker protocol 5 -> 6. Made the action dock opaque and reserved scroll space. Recorded the clipped optical symbol (`65ced2b`) as a contributing, not causal, factor. | `npm run typecheck` PASS; `npm run mobile-web:typecheck` PASS; `npm test` **52 files / 885 tests PASS** (was 862); `npm run mobile-web:test` **28 files / 415 tests PASS** (was 385) - **+53 tests**; both production builds PASS; doctor PASS 0 warnings; drift PASS. New coverage includes a stalled transfer leaving RECEIVING, partial state surviving a stall, recovery completing a transfer the initial pass could not with matching SHA-256, a segment whose every source symbol was lost, a 400-frame burst, acquisition on a later manifest, the final frame never arriving, targeted resume touching exactly the missing segments, and a failed export returning to COMPLETE. | **COMPLETE at code level - NOT ACCEPTED, PHYSICAL IOS TRANSFER GATE PENDING.** No physical device was available; all nine size tiers and all twelve provoked behaviours are PENDING and no PASS was inferred from any automated result. Certified maximum transfer size remains 0 bytes. Phase 11 may be rerun; Phase 12 may not, until the physical matrix has been executed against a recorded artifact built from this work. |
| 2026-08-23 | PERF-PHASE-12 | QA / System Architect / Front-end / Back-end | Executed Phase 12 of the Large-File / Maximum-Speed program: final integration, migration, regression validation, documentation and the release verdict. **Found and fixed WEB-IOS-SHELL-018 by running the built PWA and driving a real release-to-release upgrade, which no test covered.** The page posted its service-worker precache list exactly once, at `navigator.serviceWorker.ready`, to `registration.active` - during an upgrade that is the *outgoing* worker, whose cache the incoming worker's `activate` then deletes. Measured after a real v2 to v3 upgrade in Chromium: the new cache held its 5 `CORE` entries and neither of the two hashed files the document names, against 11 on a clean install. That is WEB-IOS-SHELL-017's permanent white page reproduced by an ordinary update. The receive-worker chunk was never in the list at all, being constructed from JavaScript and present in no element. Fix: `mobile-web/src/main.tsx` re-posts on `controllerchange` and on `load`, addresses `navigator.serviceWorker.controller` rather than `registration.active`, and builds the list from the union of the document's declared graph and `performance.getEntriesByType('resource')`; `mobile-web/public/sw.js` bumped to `deqr-mobile-shell-v4` and now excludes the health probe from `PRECACHE_URLS`. Corrected the stale v1 claim in `receive-pipeline.ts`'s module comment. Rewrote `.ai-team/engineering/ARCHITECTURE.md`, which was still a Milestone-1 proposal describing v1 only with a source tree that does not exist; corrected `PROTOCOL-V2.md`'s status header and rewrote its Sections 10 and 12; added Section 6.4 to `SECURITY.md`; added `.ai-team/engineering/TROUBLESHOOTING.md` and `RELEASE-NOTES.md`; documented the shell-cache upgrade path in `mobile-web/ARCHITECTURE.md` and corrected its stale 'no real OPFS has been exercised' boundary. Added `pwa-prod` to `.claude/launch.json`. **v1 retirement was considered and refused**, its written precondition being a certified v2 path that does not exist. | `npm run typecheck` PASS; `npm run mobile-web:typecheck` PASS; `npm test` **51 files / 842 tests PASS**; `npm run mobile-web:test` **28 files / 385 tests PASS** (378 before; +7 added here); `npm run build` PASS; `npm run mobile-web:build` PASS; `npm run doctor` PASS 0 warnings; `npm run drift-check` PASS; `git diff --check` clean. Electron dev launch emitted `DEQR_RENDERER_READY dashboard=DEQR_OPTICAL_TRANSFER preload=available`; graceful close exited **code 0 with no output after the readiness marker**, so the historical post-close exception has not returned. 19 IPC channels in main and 19 in preload, identical name for name. PWA production build in Chromium: clean first load (mount at 254 ms), worker registered and controlling, v2 to v3 to v4 upgrade each evicting the previous cache, post-fix cache holding all 11 entries with `missingFromCache: []`, and with the host **stopped** the document and all seven referenced assets served `200` from cache with full-length bodies. No horizontal overflow at 390x844 on the home or camera-recovery surfaces. Real-OPFS harness re-run: **10 checks, 0 failed**, including interrupt-and-resume across two pipelines and the retention policy. Not executed and recorded as such: custom window controls and a live-transfer lifecycle (native-window clicks), the offline top-level navigation (Chromium fails a connection-refused navigation ahead of the worker), and every physical iPhone gate. | **COMPLETE - CONDITIONALLY ACCEPTED for internal distribution; NOT ACCEPTED for release.** Committed the whole program as `940e1d2` on `phase-12-release-gates` and rebuilt with `npm run release`: portable `C0EA1571...073DE`, NSIS `8E3B6124...5785F`, `app.asar` `52209053...C5CB8` - **the first artifact that contains the program at all**, every earlier build predating it. Packaged validation all executed against it: `release:verify` matches HEAD, `test:packaged` PASS, all six Electron fuses PASS from the binary, packaged launch emitted the readiness marker and exited **code 0 with nothing after it**, and extraction confirmed `dist/pwa` ships the v4 worker, `boot.js` and every asset the packaged `index.html` names. Two independent reasons block ACCEPTED: the certified maximum transfer size is still 0 bytes with all seven physical gates PENDING, and the default transport profile is still uncertified. |
| 2026-08-23 | PERF-PHASE-11 | QA / Performance / System Architect / Back-end | Executed Phase 11 of the Large-File / Maximum-Speed program: end-to-end benchmarking, stress testing and certification. Added `scripts/bench/phase11-certification.ts` (six modes: ladder, channel, interrupt, backpressure, faults, profiles) driving the **shipping** sender, frame serializer, FEC, receiver pipeline, segment store and streaming SHA-256 through a modelled optical channel; and `scripts/bench/browser/` (page + worker + isolated Vite root) driving the shipping `ReceivePipeline` and `ReceiverStorage` against a **real Chromium OPFS** with no injected environment. Fixtures are positional generators, so a 4 GiB run needs no disk and the expected digest is computed independently of the sender - every row compares three separately computed SHA-256 values. **Results.** 35 hash-gated transfers from 1 KiB to 4 GiB across three data classes, all verified byte-exact, with receiver held bytes flat at **1.34 MiB from 10 MiB to 4 GiB** and the sender at 1.37 MiB against a 1.39 MiB declared budget. Balanced measured at **4,631 verified bytes/sec**, of which **43% of the optical link carries repair symbols nobody needed** - removing that unconditional overhead is worth 1.78x and is recorded as a finding for a later phase, not implemented. Loss up to Balanced's declared 20% design rate completes in one pass at a 5% cost; 30% needs **17 passes at 280 B/s**; and above `(1+r)(1-p) >= 1.05`, i.e. p > 0.40, no number of passes can complete a transfer, confirmed by a 50%-corruption run that never recovered a segment in 20 passes. No corrupt frame was accepted anywhere in the phase. A resume from a token read off a fresh pipeline cost **1.06x** one clean run. Six refusal paths failed closed and none claimed a digest, including a quota failure the pre-sizing truncate converts into an up-front refusal. Ten real-OPFS checks passed in Chromium including the **export handoff** that Node `fs` structurally cannot test, and real OPFS measured ~100x slower than Node `fs`, which makes every storage rate quoted in Phases 06-08 an upper bound rather than a device number. **Gates left open, deliberately.** No physical iPhone row was executed, so the certified maximum transfer size is **0 bytes** and no maximum-size or maximum-speed claim may ship; `PHASE-11-PHYSICAL-CERTIFICATION-MATRIX.md` is the instruction set, with seven named gates and every row marked PENDING. The default profile is also uncertified: Balanced needs >=3.5 camera px/module and Reliable survives 2.5, and which should be default depends on a constant nobody has measured. | `npm run typecheck:phase11` (new, both harnesses) PASS; `npm run typecheck` PASS; `npm run mobile-web:typecheck` PASS; desktop `npm test` **842 PASS / 51 files**; PWA `npm run mobile-web:test` **378 PASS / 28 files**; `npm run doctor` PASSED (0 warnings); `npm run drift-check` PASSED. Benchmark evidence: 9 machine-readable result files in `.ai-team/reports/performance/phase11/`. No file under `src/`, `mobile-web/src/`, `tests/`, `mobile-web/tests/` or `protocol/` was modified, so the suites are identical to the counts Phase 10 closed on. | PASSED - pipeline certified to 4 GiB; physical gate open and explicitly unclaimed. |
| 2026-08-22 | PERF-PHASE-10 | Security / Back-end / System Architect / QA / Front-end | Executed Phase 10 of the Large-File / Maximum-Speed program: wrote a v2 threat model tied to actual code paths (`.ai-team/engineering/SECURITY.md` §6, thirteen threats each naming the file that enforces its control), centralized every receiver-side maximum in the new `src/core/receiver-policy.ts`, and hardened the seams the audit found - the parser, FEC caps and compression container were audited and needed no change. **Four defects fixed, all at joints between hardened components.** (1) The OPFS payload allowlist named only `data.part` while the receiver writes two payload files, so every compressed transfer received to OPFS had its `verified` message silently discarded by the main thread's own guard after scanning, recovering, decompressing and passing SHA-256; `export.ts` would have refused to open it for the same reason. Added `isReceiverSessionFile`, the closed set of `data.part` and `original.part`, with `checkpoint.json` still excluded. (2) Manifest-level refusals set only a rejected-frame reason, and `progress().fault` is the only route from the pipeline to a screen - so a blocked file type or an undecompressable transfer left a phone scanning silently forever while Phase 09's copy for exactly that sat unreachable in `faultCopy`; every `beginV2Session` refusal is now a terminal session fault. (3) `loopback:start` read a renderer-supplied options object inside a `setInterval` callback, where a throw reaches no caller and surfaces as Electron's main-process error dialog; arguments are now typed and clamped once at the IPC boundary via `asSessionId`/`readLossPercentage`, and no renderer structure reaches a timer. (4) A checkpoint on the device was read and base64-decoded with no size bound and the length check after the allocation; both are now bounded by `RECEIVER_POLICY.maxCheckpointBytes` before the decode. Also: a manifest above the receiver's segment budget reached `SegmentedReceiver`'s constructor inside the provisioning callback and reported `CHECKPOINT_INCONSISTENT`, now refused by `manifestPolicyRefusal` before any storage call with zero bytes created; a renderer-supplied `defaultName` reached `showSaveDialog`'s `defaultPath` unsanitized; and `submit` gained a frame-length gate ahead of the O(n) fingerprint. Added `trackedRepairIds` so `maxTrackedRepairIds` can be asserted rather than stated. Four new test suites cover the brief's whole input list: boundary values, offset/count overflow, random bytes, duplicate storms, pathological FEC metadata, decompression bombs, malicious filenames, checkpoint tampering, unsupported profile/version, and cancel during a hostile stream. | `npm run typecheck` clean; `tsc -p tsconfig.main.json --noEmit` clean; `npm run mobile-web:typecheck` clean; `npm test` **842 PASS / 51 files** (from 789 / 48); `npm run mobile-web:test` **378 PASS / 28 files** (from 347 / 27); `npm run build` clean; `npm run mobile-web:build` clean (receive worker 218.23 kB chunk); `npm run test:packaged` PASS; `npm run vectors:v2:generate` 24 vectors written and the 52 vector assertions pass unchanged; `npm run doctor` PASSED (0 warnings); `npm run drift-check` PASSED. Runtime: six IPC handler signatures changed, so Electron was launched against the Vite dev server and reported `DEQR_RENDERER_READY dashboard=DEQR_OPTICAL_TRANSFER preload=available` with the renderer console at debug/info only - the `loopback:start` crash itself was **not** reproduced in the live app (it needs a deliberately malformed call from inside the renderer) and rests on the executed handler test instead. Memory and CPU bounds asserted against decoder-reported counters rather than wall-clock: 50,000 identical repair frames leave `pendingEquations`, `pendingNeighborRefs`, `xorBytes` and `heldBytes` unchanged from after the first; 20,000 distinct ids the algebra cannot consume saturate with 19,000+ refusals; 10,000 frames at a completed segment add zero XOR bytes; 5,000 header-corrupted frames never report `sessionComplete`; 64 MiB of zeros gzipped into a 64 KiB window fails as `DECOMPRESSED_SIZE_MISMATCH` with the sink still one window. | SUCCESS - gate PASSED; no known malformed input path triggers unbounded allocation, unbounded queue growth, a long CPU loop, arbitrary filesystem access, or a false verified state. **Nothing on the wire changed** (no edit to `protocol-v2.ts`, `protocol.ts` or `container.ts`) and **the worker protocol stays at 5**, so a cached Phase 09 shell still handshakes. Phase 10 adds **84 tests** in four new files and modifies no existing test; no prior assertion was weakened. Four deviations stated: no dedicated fuzzer (seeded property testing instead, which explores less than a coverage-guided run); **no benchmark was re-run**, so the performance claim is an argument from the shape of the change and is labelled as one; v1's `LIMITS` deliberately not folded into the receiver policy; and no concurrent-session cap on `StreamingSessionRegistry`, accepted because every session costs a modal file dialog. Physical iPhone certification remains Phase 11's. |
| 2026-08-22 | PERF-PHASE-09 | Front-end / UI-UX / System Architect / Back-end / QA | Executed Phase 09 of the Large-File / Maximum-Speed program: rebuilt both transfer UIs around the segmented, resumable, compressed architecture, and wired the desktop sender to it — it was still driving **v1**, so every part of Phases 02–08 sat behind an IPC surface the renderer never called. Added `src/shared/transfer-ui-state.ts`, the one phase vocabulary both surfaces describe a transfer in, whose `SENDER_PHASES` set structurally excludes `VERIFIED` and `EXPORTING` so no sender state can claim an integrity check it did not perform. Added `src/renderer/sender-state.ts` (ten states, transition table as data, every UI question derived) and deleted `state-machine.ts` — an `AppStateMachine` nothing rendered from that cast `'receive-camera'` through `any` — along with the fifteen-member `TransferState` union it was typed against. Added `src/renderer/sender-model.ts`: `bigint` byte formatting past `MAX_SAFE_INTEGER`, a measured rate, and an `EtaEstimator` that names four reasons for withholding a prediction and shows a rate before it will show one. Rewrote `App.tsx` onto `streamTransfer`; added `SenderPreflightCard` (file, both sizes, compression decision, Balanced-default profile selector), `StreamTransferView` (pulled frames, one dominant status, collapsible diagnostics) and `ResumeTokenEntry`, closing the desktop half Phase 07 left open; reduced `QRCanvas` to the presentational canvas so the never-animate-the-symbol rules have one owner. Removed the 32 MiB ceiling messaging, which stopped being true at Phase 02. Extended `streamTransfer.select` to `{ resumeToken, transportProfileId }` with `resolveTransportProfile` refusing unknown and non-production ids on the privileged side. On the receiver: added `receiver-view-model.ts`, mapped Phase 05's states onto the shared vocabulary, plumbed the `StoragePreflight` that `onProvisioned` had been **discarding** into `ReceiveProgress` (protocol 4 → 5), subscribed to the `onVerifyProgress` event Phase 08 emitted to nobody, and gave screens to the session metadata, the storage decision, adopted segments, checkpoint rejections, two-phase verification, the interrupted session's kept bytes and resume code, and the `UNSUPPORTED_COMPRESSION` refusal — which has no back channel, so its sentence is the entire remedy. Added `estimateDeviceStorage` and `discardRetainedSessions`. Seven test suites added. | `npm run typecheck` clean; `npm run mobile-web:typecheck` clean; `npx vitest run` 789 PASS / 48 files; `npm run mobile-web:test` 347 PASS / 27 files; `npm run build` clean; `npm run mobile-web:build` clean (receive worker 215.96 kB chunk); `npm run test:packaged` PASS; `npm run vectors:v2:generate` deterministic, 24 vectors, `git diff --exit-code` clean; `npm run doctor` PASSED (0 warnings); `npm run drift-check` PASSED. Runtime: Electron against the dev server reported `DEQR_RENDERER_READY dashboard=DEQR_OPTICAL_TRANSFER preload=available`; the PWA at 375x812 mounted with `scrollWidth === clientWidth === 375` and rendered a real measured storage line. | SUCCESS — gate PASSED; every transfer, error, resume and verification state is explicit and testable, and no UI can imply success before integrity verification because the sender's phase set does not contain the phases that make the claim. **Two defects found by running the app, neither reachable from a unit test**: the frame scheduler was keyed on inline callback identities and would have been rebuilt twice a second off the progress poll, and the frame source set React state per painted frame — twelve full re-renders a second on the thread that paints the QR symbol. Both fixed and pinned. One bug found while writing a test: `discardRetainedSessions` passed the sessions directory where the sweep expects the OPFS root and deleted nothing. Six deviations stated. Five tests deleted with the dead module they covered and two removed with the functions they tested; every assertion carried forward to the module that ships, and no prior assertion weakened. Physical iPhone certification remains Phase 11's. |
| 2026-08-22 | PERF-PHASE-08 | Back-end / System Architect / QA / Security / Front-end | Executed Phase 08 of the Large-File / Maximum-Speed program: removed every route by which a filename could reach a transport decision, and made compression something the bytes decide. Added `src/core/compression-policy.ts` — the decision function takes a size, a bounded sample and a policy, and has **no parameter that could carry a name, an extension, a MIME type or a path**, which is how the program's no-extension-specific-transport rule is now held. Defined the GZIP transport container in `src/core/protocol-v2.ts` (`[u32BE length][gzip member]` per fixed run of original bytes; `compressionParam` becomes log2 of the window, 16..26; window plan, original-range math, and one shared validator so the serializer and parser cannot drift) and specified it in `PROTOCOL-V2.md` §4.5. Added `src/main/window-compressor.ts`: one window and one record held at a time, a counting `measure()` walk that yields an exact `transportSize` without the compressed file existing anywhere, an `onWindow` hook that fuses the walk into the SHA-256 pass, and arbitrary transport-range reads for resume. Wired it through `streaming-sender.ts` (sample → decide → fused sizing walk → confirm or fall back to original bytes; four new config fields; compressed segments read from the container). On the receiver: `mobile-web/src/inflate-verify.ts` expands a container under manifest-derived bounds with five typed refusals, `opfs-original-sink.ts` and `BoundedMemoryOriginalSink` hold `original.part`, `opfs-segment-store.ts` reserves and pre-sizes it at session start, `receiver-storage.ts` requires room for both files, and `receive-pipeline.ts` accepts GZIP manifests and verifies in two named phases. Bumped the worker protocol 3 → 4 for the verify phase and the two sizes, and widened `StreamingTransferMetadata`/`StreamingProgressView` so the desktop IPC boundary carries the transport size and the applied decision beside the original size. Regenerated the v2 golden vectors (byte 42 gained a meaning; two new rejection vectors). Added `scripts/bench/phase08-compression.ts` (seven modes) and five test suites. | `npm run typecheck` clean; `npm run mobile-web:typecheck` clean; `npm run mobile-web:build` clean (receive worker 215.59 kB chunk); `npx vitest run` 696 PASS / 45 files; `npm run mobile-web:test` 282 PASS / 24 files; `npm run vectors:v2:generate` deterministic, 24 vectors; phase08 bench modes corpus, levels, threshold, neutral, window, memory and receiver captured to `.local-run/bench/phase08/` | SUCCESS — gate PASSED; identical bytes under `.txt`/`.zip`/`.pdf`/`.xlsx`/`.bin` produced one decision five times out of five for both compressible and incompressible content; real source compressed 0.269 for a **3.72× optical speed-up**, high-entropy bytes bypassed entirely; sender peak buffered 2.51 MiB against a 3.39 MiB budget, unchanged from 64 MiB to 256 MiB; receiver pays 5–8 s/GiB to decompress and holds `original + container` until sweep; seven deviations stated; no new dependency taken; neither shipping UI wired, which is Phase 09's |
| 2026-08-21 | PERF-PHASE-07 | Back-end / System Architect / QA / Security / Front-end | Executed Phase 07 of the Large-File / Maximum-Speed program: made long transfers recoverable without weakening what decides whether one is real. Added `src/core/resume-token.ts` — a 40-character Crockford base32, CRC-guarded token carrying session id, file id, segment count, a 5-byte digest prefix and the lowest segment still needed, because a one-way optical channel has no acknowledgement path and the user is the only return route. Added checkpoint adoption: `matchCheckpoint`/`readCheckpointEntry` in `mobile-web/src/opfs.ts` (six typed rejections, full-field validation, "absent" separated from "unreadable" so a corrupt checkpoint's directory is cleared), resume-aware `OpfsSegmentStore.open` (adopt, refuse, or clear; `state: 'complete'` when the last segment lands), `adoptedSegments`/`firstMissingSegment()` on `SegmentedReceiver`, and `resume` plumbed through `receiver-storage.ts` and `receive-pipeline.ts`. Made retention a function of how a session ended (`SessionEndReason`: interrupted retains, cancelled/failed discard) and destroyed working data on `HASH_MISMATCH`. Added a `verify-progress` event reporting the hash apart from the transfer; bumped the worker protocol 2 → 3 for the resume flag, the session-ending reason and that event. Added sender-side resume: `resumeToken` config, `applyResume` and `senderResumeToken` in `streaming-sender.ts`, carried through the registry, IPC and preload as `streamTransfer.select(resumeToken?)`. Added `scripts/bench/phase07-resume.ts` and five test suites. | `npm run typecheck` clean; `npm run mobile-web:typecheck` clean; `npm run mobile-web:build` clean (receive worker 206.55 kB chunk); `npm test` 643 PASS / 41 files; `npm run mobile-web:test` 262 PASS / 23 files; phase07 bench modes resume (256/1024/4096 MiB at 1/50/90/99%), adopt (32/128/1024/4096 MiB) and saving captured to `.local-run/bench/` | SUCCESS — gate PASSED; 4 GiB resumed at 90% adopting 1,658 segments in 5.55 ms from an 810-byte checkpoint and verified hash-identically; all 1,240 single-character token substitutions rejected; seven deviations stated; interruption now retains partial data (privacy-posture change, argued in the report); no per-segment digest and no UI for the sender's resume, both deliberate |
| 2026-08-21 | PERF-PHASE-06 | Back-end / System Architect / QA / Security / Front-end | Executed Phase 06 of the Large-File / Maximum-Speed program: moved the PWA receiver's working storage to OPFS so its memory stops being a function of the file. Added an incremental SHA-256 (`src/core/sha256-stream.ts`) because `crypto.subtle.digest` cannot be fed a file the receiver never holds; added `mobile-web/src/opfs.ts` (capability detection, sync-handle probe, quota preflight with margin, session layout, checkpoint metadata, retention sweep), `opfs-segment-store.ts` (one pre-sized file per transfer, exclusive sync access handle, plan-derived offset writes, coalesced checkpoints) and `receiver-storage.ts` (storage chosen at the manifest, refused before scanning when there is nowhere to put the file). Rewrote `segment-store.ts` so `write` reports which refusal; rewrote `receive-pipeline.ts` for asynchronous provisioning with a `pending-storage` outcome, generation-fenced cancellation, streaming verification and retention on reset; rewrote `export.ts` to hand over a `File` referencing the OPFS entry rather than a copy; bumped the worker protocol 1 → 2 for the new `verified` source descriptor. Added `scripts/bench/phase06-opfs-storage.ts`, an OPFS test double, and two test suites. | `npm run mobile-web:typecheck` clean; `npm run typecheck` clean; `npm run mobile-web:build` clean (receive worker 198.86 kB chunk); `npm run mobile-web:test` 206 PASS / 20 files; `npm test` 609 PASS / 39 files; phase06 bench modes receive (32/128/1024/4096 MiB), compare (16/64/256 MiB) and hash captured to `.local-run/bench/` | SUCCESS — gate PASSED; 4 GiB received at 125 MiB peak JS growth and 0 B store residency; nine deviations stated; no real OPFS exercised and no iOS export size limit claimed, both Phase 11 |
| 2026-08-21 | PERF-PHASE-05 | Front-end / Back-end / System Architect / QA / Security | Executed Phase 05 of the Large-File / Maximum-Speed program: rebuilt the PWA receiver capture/decode pipeline. Moved frame parsing, CRC, dedupe, fountain and segmented recovery, container parsing and whole-file SHA-256 off the main thread into the receive worker; made the in-flight cap (2) the queue bound under a one-terminal-event-per-frame contract; replaced five contradicting receive-screen flags with one authoritative state machine; added a versioned and guarded worker message schema, bounded fingerprint dedupe, a `SegmentStore` seam for Phase 06, and capture-side telemetry including the `symbolSpanPx` observation Phase 04 left outstanding. Added `worker-protocol.ts`, `receiver-state.ts`, `frame-dedupe.ts`, `segment-store.ts`, `receive-pipeline.ts`, `receive-worker-core.ts`, `receive-worker.ts`, `receiver-client.ts`, `metrics.ts`; rewrote `camera.ts` and `App.tsx`; removed `decoder.ts` and `decoder.worker.ts`; added `scripts/bench/phase05-capture-pipeline.ts` and five test suites. | `npm run mobile-web:typecheck` clean; `npm run mobile-web:test` 159 PASS / 18 files; `npm run mobile-web:build` clean; `npm run typecheck` clean; `npm test` 600 PASS / 38 files; phase05 bench modes offload / sustained / backpressure / split / dedupe captured to `.local-run/bench/` | SUCCESS — gate PASSED, six deviations stated, physical iPhone certification remains Phase 11 |
| 2026-08-21 | PERF-PHASE-04 | Back-end / Front-end / System Architect / QA | Executed Phase 04 of the Large-File / Maximum-Speed program: built a measured QR transport-profile system, fixed the render path, and added a paced bounded frame scheduler. **The finding that drove everything: the QR version that maximises throughput is decided almost entirely by camera pixels per module, which nobody has measured on a device.** Added `src/core/qr-capacity.ts` (all 40 versions x 4 ECC levels derived from the shipping encoder, plus integer module-scale geometry) and `src/core/transport-profiles.ts` (Reliable/Balanced/Turbo/Experimental, the measured decode-success surface they were read off, and a validator). `scripts/bench/phase04-qr-profiles.ts` renders real v2 frames, simulates a camera looking at them - area-average resample at a fractional offset, Gaussian defocus, sensor noise, symbol embedded in a fixed 1280x720 capture frame - decodes with the receiver's own jsQR, and composes decode success with Phase 03's repair curve into verified payload bytes per second. At 3 px/module only version 10 is usable at all; at 5 px/module version 24 decodes every frame. **ECC was measured and rejected as a lever**: L to H moved decode success by at most 5 points, H was worse than L, and it cost 58% of the payload at v20 - the failure mode at low density is that the module grid cannot be resolved, and error correction does not repair a grid never read. Every profile is ECC L. **Fixed a real render defect on the shipping v1 path**: `width: 400` across 97 modules is 4.12 device pixels each, so module widths alternated between four and five and every edge fell on a fractional boundary, then CSS resampled the canvas again and HiDPI a third time. Now an integer module scale with an explicit CSS size; proven by a test that passes on the new path and fails on the old one. Added `src/renderer/qr-frame-scheduler.ts`: frames pulled not pushed, bounded prefetch, a minimum frame-hold floor that wins over a faster target, measured rather than nominal FPS, no catch-up bursts, and no timer that outlives its owner - all asserted against an injected clock. Manifest byte 43 changed from `reserved` (MUST be zero) to `transportProfileId` (advisory, unknown values reported not rejected); **every golden vector generated before the field existed is byte-identical after it**, and a new vector pins the new semantics. `configFromProfile` is now the only place symbol size, segment size and repair overhead are decided, so the three numbers that set throughput can no longer disagree. **Physical certification is PENDING**: `physicallyCertified` is false for every profile and a test asserts it; `PHASE-04-PHYSICAL-TEST-MATRIX.md` specifies the Phase 11 runs, the telemetry schema, pass criteria, and - written in advance - four findings that would falsify a Phase 04 decision. Two harness artefacts were found and fixed mid-phase and are recorded because each changed a conclusion: `drawImage` aliasing at non-integer resample ratios, and cells reusing eight frames across forty trials. | `npm test` **600 PASS / 38 files** (from 510/34); `npm run mobile-web:test` **77 PASS / 12 files** (unchanged); both typechecks PASS; `npm run build` PASS; `npm run mobile-web:build` PASS; `npm run test:packaged` PASS; `npm run vectors:v2:generate` 22 vectors reproducible; `npm run doctor` PASS (0 warnings); `npm run drift-check` PASS (zero drift); benchmark sweeps: capacity across 160 version/ECC cells, optical robustness across 9 versions x 5 sampling densities x 60 independent frames, ECC comparison across 2 versions x 4 levels, CPU across 8 versions, goodput across 3 versions x 3 densities x 5 cadences | **PASS** - gate met with executed evidence; physical certification explicitly deferred to Phase 11 rather than invented. Seven deviations stated in the report, incl. FPS targets set below the plan's range because the measured jsQR scan ceiling is 11-17 FPS. Report: `.ai-team/reports/performance/PHASE-04-QR-TRANSPORT-PROFILES-REPORT.md` |
| 2026-08-20 | PERF-PHASE-03 | Back-end / System Architect / QA / Security | Executed Phase 03 of the Large-File / Maximum-Speed program: turned the v2 FEC path into a systematic-first, segment-scoped recovery engine. Added `src/core/segment-decoder.ts` (source symbols written straight into the segment store, algebra reached only for missing symbols, belief propagation driven by a dependents index rather than v1's full scan, every attacker-influenceable quantity capped) and `src/core/segmented-receiver.ts` (manifest binding and validation, routing, at most `maxActiveSegments` live decoders, commit-and-release with buffer ownership transferred to the sink, one completion bit per segment as the only file-proportional state). Added a `RepairNeighborFn` seam to `src/core/segment-encoder.ts` so candidate FEC profiles are measured through the real encoder and decoder. **A loss-free segment now costs exactly zero XOR operations** - asserted as an integer, not characterised - and decodes at 1,302 MiB/s against v1's 342 MiB/s; under 30% loss v2 is 1.5x slower than v1 at 39 MiB/s, which is still a 7,900x margin over the 5,120 B/s optical link Phase 00 measured. **Settled the number Phase 02 left open.** `scripts/bench/phase03-fec.ts` emits a systematic pass then repair until the segment closes and records the ratio that took: p99 0.46 at 1% loss, 0.71 at 5% and 10%, 0.72 at 20%, 0.94 at 30% for the default 1 MiB/512 B profile. The inherited default of 0.05 recovers nothing above 0% loss and was raised to 0.75, which covers every rate to 20% at p99 and costs a clean transfer 75% more frames - a stated operating point, not an optimum, because a segment must close inside the pass it is received in until Phases 06/07 add storage and checkpoints. Two alternative degree rules were measured and rejected: a geometric degree ladder is 5.8x cheaper at 1% loss but failed 40/40 trials at 20% and 30% under bounded memory and still needs p99 1.54 against 0.94 with the caps removed; fixed degree 3 is worse everywhere. Robust soliton was the only rule closing every trial at every rate, so FEC profile `0x01` is unchanged and no dependency was added. Also measured: burst loss costs the same as independent loss, and symbol size matters to recovery efficiency where segment size does not (2 KiB symbols need 1.55 at 30% against 512 B symbols' 0.94) - handed to Phase 04. Two micro-optimisations were tried, measured as no-change, and reverted rather than kept on theory. Normative FEC profile written into `.ai-team/engineering/PROTOCOL-V2.md` sections 7.2/7.3. No v1 transfer file was modified and neither shipping UI is wired to v2 (Phases 04/05). | `npm test` **510 PASS / 34 files** (from 439/31); `npm run mobile-web:test` **77 PASS / 12 files** (from 71/12); `npm run typecheck` PASS; `npm run mobile-web:typecheck` PASS; `npm run build` PASS; `npm run mobile-web:build` PASS; `npm run test:packaged` PASS; `npm run doctor` PASS (0 warnings); `npm run drift-check` PASS (zero drift); `scripts/bench/phase03-fec.ts` sweep across 5 segment/symbol configurations x 6 loss rates x 2 loss patterns, and v1-vs-v2 comparison at 40 trials per rate | **PASS** - phase gate met with executed evidence; six deviations stated in the report, incl. the clean-channel cost of the new default and the unexplained lossy-path slowdown. Report: `.ai-team/reports/performance/PHASE-03-SYSTEMATIC-FOUNTAIN-REPORT.md` |
| 2026-08-20 | PERF-PHASE-02 | Back-end / System Architect / QA / Security | Executed Phase 02 of the Large-File / Maximum-Speed program: replaced whole-file sender preparation with a streaming, segmented, bounded-memory v2 pipeline. Added `src/core/segment-encoder.ts` (segment-scoped systematic-first symbolizer, browser-safe), `src/main/streaming-sender.ts` (preflight, bounded segment reads, bounded ready queue, lifecycle), `src/main/streaming-session-registry.ts` (privileged ownership), four `streamTransfer:*` IPC channels through the existing trusted wrapper, the matching preload bridge, and shutdown teardown on window close, renderer loss and quit. Measured v1 against v2 over identical corpora with the settled-live-bytes metric Phase 00 validated: v1 holds exactly 2.00x the file at 1, 8, 16 and 31 MiB; v2 holds 1,217,519 B at 1 MiB and 1,339,042 B at 1 GiB, with `memoryBudgetBytes()` a pure function of configuration at 1,099,414 B and `bufferedBytes()` never exceeding it. A synthetic 1 GiB stream passed all 262,272 frames through the real pipeline against a file that never existed on disk. Frames are pulled rather than pushed, so backpressure is structural and there is no main-process timer of the kind that previously outlived a destroyed renderer. The largest IPC response is one symbol plus 32 header bytes, and the metadata crossing the boundary is asserted to disclose no path. A session over a file just past 2^32 proved the sender issues 64-bit read positions. Compressibility is sampled and deliberately not applied (Phase 08); repair symbols are produced with the XOR identity asserted, but the degree distribution, the 0.05 overhead ratio and any recovery guarantee are Phase 03's. The shipping UI still drives v1 - display cadence is Phase 04 and transfer UX is Phase 09 - and no v1 transfer file was modified. Fixed one real defect found in the process: `hashChunkBytes` and `compressibilitySampleBytes` were validated against the segment size they share a buffer with, so a small segment plus the default 1 MiB hash chunk failed with an error about a knob the caller never set; both are now validated against the protocol maximum and clamped at use. | Desktop `npm test` **439 PASS / 31 files** (up from 395 / 28); PWA `npm run mobile-web:test` **71 PASS / 12 files** (unchanged); desktop, main, preload and PWA typechecks, both production builds, `npm run test:packaged`, doctor (0 warnings), drift and `git diff --check` all PASS. Memory evidence reproducible with `node --expose-gc node_modules/vite-node/vite-node.mjs scripts/bench/phase02-sender-memory.ts -- --path v1|v2 --mib N`. Three defects in the new tests themselves were found by running them rather than reading them, including one that passed its `rejects.toThrow()` assertion for entirely the wrong reason - an invalid config meant it exercised a config error instead of the read failure it claimed to cover. | PASS - PHASE 02 GATE MET; PHASE 03 (SYSTEMATIC FOUNTAIN) UNBLOCKED |
| 2026-08-20 | PERF-PHASE-01 | System Architect / Back-end / QA / Security | Executed Phase 01 of the Large-File / Maximum-Speed program: defined and implemented the DEQR v2 binary protocol. Added `.ai-team/engineering/PROTOCOL-V2.md` (normative byte-layout tables, constraints, error model, migration), `src/core/protocol-v2.ts` (types, serializer, parser, segmentation, 64-bit helpers), `src/core/crc32.ts` (CRC-32/ISO-HDLC), a deterministic vector generator, and 21 golden vectors in `protocol/test-vectors-v2/`. v2 replaces v1's model rather than stretching it: a one-time session manifest carries filename, SHA-256, sizes, segmentation, compression mode and FEC profile, and compact data frames carry session/file identity, a live u32 `segmentIndex`, `symbolId`, `sourceSymbolCount`, payload and a whole-frame CRC-32 in 32 bytes of overhead. Sizes and offsets are `bigint` end to end with `toSafeNumber` throwing rather than rounding above `Number.MAX_SAFE_INTEGER`; a 5 GiB transfer is described by a 126-byte manifest with nothing of that size allocated, and a segment offset of 2^26 x 3,999,999,999 is computed exactly. Decoder state is bounded by a segment, asserted by comparing a 5 MiB and a 5 TiB plan. The parser never throws: every malformed input is a typed `DeqrV2Error`, and v1 frames are reported as `V1_FRAME` rather than as corruption. Went beyond the plan deliberately on one point: the codec is `Buffer`-free, so the Electron sender and the iOS PWA share one implementation instead of the two v1 forced. Deviated on two: data frames omit the logical file offset (derived from the manifest; eight bytes per symbol against a 5,120 B/s link) and the spec lives under `.ai-team/engineering/` per AGENTS.md rather than the repository root. Nothing is wired into the sender or receiver - v1 remains the shipping wire format and no v1 file was modified. | Desktop `npm test` **395 PASS / 28 files** (up from 286 / 25); PWA `npm run mobile-web:test` **71 PASS / 12 files** (up from 63 / 11); both typechecks, both production builds, `npm run test:packaged`, doctor (0 warnings) and drift all PASS. `npm run vectors:v2:generate` byte-identical across regeneration, now enforced in CI by `git diff --exit-code`. About 20,000 fuzz inputs with zero escaped throws. Browser-safety proven through the production Rollup path rather than vitest alone: a 20,691-byte ES module with zero Node imports and zero `Buffer` references. A latent defect in the fuzz harness itself was found and fixed - a power-of-two LCG's low bits made one whole arm of the code-coverage sweep never run, caught only because the test asserts which rejection codes it reached. | PASS - PHASE 01 GATE MET; PHASE 02 (DESKTOP STREAMING SENDER) UNBLOCKED |
| 2026-08-20 | PERF-PHASE-00 | Project Manager / System Architect / QA | Executed Phase 00 of the Large-File / Maximum-Speed program: baseline audit, limit discovery, and a reusable measurement harness. Established by runtime probe, not source reading, that the ~32 MB ceiling is a field width rather than a size constant: the frame header carries `blockCount` as `uint16` (`src/core/protocol.ts:69`) against a fixed 512-byte sender block (`src/main/session-manager.ts:14`), so the entire v1 transport capacity is 512 x 65,535 = 33,553,920 bytes. The boundary probe walked it exactly - 33,553,818 source bytes serialize to precisely 33,553,920 and give K=65,535; one byte more is rejected and the encoder throws `Invalid block count K=65536`. A nominal 32 MiB file is 512 bytes past the ceiling and is refused before it is even read. `segmentNumber` exists as `uint16` but is hard-wired to 0 (`src/core/fountain-encoder.ts:88`) and rejected non-zero by the receiver, so v1 cannot be widened from inside. Next walls recorded: `MAX_FILE_SIZE`/`LIMITS.maxFileBytes` at 64 MiB, then `totalPayloadLength` as `uint32` at 4 GiB-1. Answered the extension-speed question with identical bytes under `.txt/.pdf/.zip/.xlsx/.bin`, five samples, running order rotated per sample: identical block counts throughout, and with filenames held to equal byte length, one distinct container digest excluding the filename field and one container length. Per-extension means fell within 0.8% / 2.6% against 21% run-to-run noise, and in 7 of 10 samples the slowest cell was whichever extension ran first. No transport, framing, FPS, compression, or QR setting reads the extension anywhere; the only extension logic is the blocked-list refusal. The real content effect is entropy and DEQR captures none of it: `compressIfBeneficial` is never called by the sender, so a corpus that gzip shrinks to 12.8% ships at full size. Also recorded: the optical link at 10 FPS x 512 B is 5,120 B/s, roughly four orders of magnitude below the slowest software stage, so a full systematic pass at the ceiling takes 1 h 49 min; the receiver has no OPFS, IndexedDB, or incremental write at all; its decoder holds 1.25-1.74x the payload with 129-380 B of object overhead per 512-byte block. Added `scripts/bench/phase00-baseline.ts` and `scripts/bench/phase00-receiver-memory.ts` and the full report at `.ai-team/reports/performance/PHASE-00-AUDIT.md`. The planned in-app telemetry stream was deliberately not built - stated as a deviation with reasoning in section 9 of the report. | `npm test` 286 PASS / 25 files; `npm run mobile-web:test` 63 PASS / 11 files; both typechecks PASS; new bench scripts typecheck clean under the project compiler options; `npm run doctor` PASSED (0 warnings); `npm run drift-check` zero drift; baseline sweep `PHASE00_BASELINE_COMPLETE capacity=10 extensionSamples=10`; receiver memory probe COMPLETE at 1/4/16/31 MiB. Suite counts unchanged, which is the expected outcome for a phase that added no shipping code. | PASS - PHASE 00 GATE MET; PHASE 01 (PROTOCOL V2) UNBLOCKED |
| 2026-08-06 | TSK-000 | Project Manager | Bootstrapped canonical `.ai-team/` system, 9 roles, 22 skills, 8 workflows, vendor adapters, and validator scripts. | System validation & file integrity check | SUCCESS |
| 2026-08-06 | TSK-010-012 | Architect / Sec / UI | Upgraded DEQR specs with formal threat model, architecture status annotations, UI/UX screens, and ADRs. | AI Doctor & Adapter Drift Check | SUCCESS |
| 2026-08-06 | TSK-020-025 | Back-end / QA | Implemented M1 Core Pipeline (LT encoder/decoder, SHA-256, protocol, compression) and initialized Node scaffolding. | npm run test, npm run typecheck | SUCCESS |
| 2026-08-07 | TSK-060 | System Architect | Expanded deterministic protocol generator to 15 binary vectors plus `expected.json`, including all K=5 systematic frames, repair frames, PRNG/Soliton expectations, and malformed session/oversize vectors. | `npm run vectors:generate`; `git diff --exit-code -- protocol/test-vectors` in GitHub Actions run 31214720792 | SUCCESS |
| 2026-08-07 | TSK-061 | Back-end / QA | Repaired `DEQR.Core` compile structure, retargeted to `net10.0`, hardened fountain bounds/duplicate handling, and expanded parity tests to require byte-perfect container/frame/encoder parity and decoder completion. | .NET SDK 10.0.302 restore/build; xUnit parity suite; AI doctor in GitHub Actions run 31214720792 | SUCCESS |
| 2026-08-08 | WEB-IOS-1..9 | Implementation owner | Superseded MAUI active strategy with separate `mobile-web/` Safari PWA receiver; added offline shell, user-initiated camera adapter, worker raw-byte QR adapter, browser-safe DEQR v1 parser/fountain receiver, hash gate, safe export fallback, and deterministic tests. | Validation evidence recorded under `artifacts/mobile-web-validation/` after execution. | IN PROGRESS |
| 2026-08-08 | WEB-IOS-10 / DESKTOP-INTEROP-001 | Implementation owner | First physical iPhone scan collected 166/166 blocks and exposed a sender contract defect: desktop sessions fountain-encoded raw source bytes rather than a DEQR container. Corrected the active-worktree sender to serialize the v1 container; made expected loopback cancellation idempotent; allowed only Vite's local HMR WebSocket in development. Added a local runbook. | `npm.cmd test` 13 files/127 tests PASS; `npm.cmd run mobile-web:test` 3 files/13 tests PASS; `npm.cmd run mobile-web:build` PASS; `npm.cmd run typecheck` PASS; AI doctor PASS (0 warnings); physical retest pending. | IN PROGRESS |
| 2026-08-09 | WEB-IOS-OPS-001 | Implementation owner | Added `scripts/run-local.ps1`, a single launcher for the Electron sender (5173) and PWA (5174), with readiness checks, optional local HTTPS, and process-tree cleanup. | PowerShell parser validation PASS; package-script JSON check PASS; AI doctor PASS (0 warnings); `git diff --check` PASS. | IN PROGRESS |
| 2026-08-09 | WEB-IOS-OPS-002 | PM, Architect, Security, Front-end, Back-end, QA, Data, UI/UX | Reproduced and remediated the Electron white-screen defect: independent Vite optimizer caches prevent the PWA from invalidating the desktop `buffer` dependency. Hardened renderer bootstrap, exact development URL policy, launcher response/renderer readiness, SAN-aware HTTPS reporting, bounded PWA decompression, receiver extension policy, and PWA camera recovery. | Independent QA: desktop 14 files/131 tests PASS; PWA 3 files/16 tests PASS; type checks, desktop/PWA builds, doctor PASS. HTTP, HTTPS, and diagnostic launcher runs each emitted the mounted-dashboard/preload marker and cleaned ports. Packaged and physical-iPhone gates NOT EXECUTED. | CONDITIONAL PASS — NO RELEASE PROMOTION |
| 2026-08-10 | WEB-IOS-UXPERF-003 | Project Manager | Reconciled the user work order with the active `codex/ios2-shell` worktree and opened read-only architecture/performance, Apple UI/UX, and QA/security validation of the existing uncommitted remediation. | `git status --short --branch`; worktree inventory; prior physical-iPhone evidence and benchmark inventory | IN PROGRESS |
| 2026-08-10 | WEB-IOS-UXPERF-003-A..F | PM / Architect / UI-UX / Front-end / QA / Security / Branding | Completed bounded sender/PWA hot-path audit, PWA and Electron Apple-inspired accessibility/state remediation, and deterministic platform icon exports. Preserved the existing dirty worktree and recorded a 14-section evidence report. | Desktop 17 files/141 tests PASS; PWA 7 files/25 tests PASS; both typechecks/builds PASS; doctor/drift/diff-check PASS; combined launcher emitted renderer-ready marker and cleaned ports; final Node proxy matrix verified all cases; 390x844 PWA local review PASS for home/recovery only. | NOT ACCEPTED - corrected physical iPhone and packaged Electron gates NOT EXECUTED |
| 2026-08-10 | DESKTOP-SEC-050 | PM / Back-end / QA / Security | Produced the Windows package (NSIS + portable + unpacked) and verified it as an artifact rather than from configuration. Added `scripts/ci/inspect-packaged-fuses.js` (reads the fuse wire from the built exe) and `scripts/ci/generate-acceptance-fixtures.js` (deterministic 5 KiB-1 MiB and representative-extension fixtures). Fixed a reproduced defect: `tests/main/permissions.test.ts` validated a hand-copied duplicate of the permission handlers that had diverged from shipped parsed-URL policy; extracted `evaluateMediaPermission` into `src/main/development-request-policy.ts` and pointed both Electron handlers and the tests at it. | All 6 required fuses read from the binary PASS; ASAR structure PASS with nothing unpacked; ASAR runtime integrity proven by 4 targeted tamper cases, each rejected before readiness; packaged smoke emitted `DEQR_RENDERER_READY` and exited clean in 4.08s with 0 blocked-network warnings; portable readiness proven by autoclose/no-autoclose control experiment; packaged CSP has no dev allowance and no inline script; preload boundary narrow. Desktop 19 files/168 tests PASS; PWA 7 files/25 tests PASS; both typechecks/builds, doctor, drift, diff-check, `test:packaged`, and combined HTTPS launcher PASS. | CONDITIONAL - all non-device criteria PASS; packaged optical transfer to a physical iPhone NOT EXECUTED |
| 2026-08-10 | WEB-IOS-10 | QA Engineer | No work executable. No physical iPhone was available to the session, so every criterion in the physical acceptance matrix remains unexecuted. Prepared the gate instead: deterministic fixtures with recorded SHA-256 digests, and a host-side launcher run advertising the PWA at `https://100.95.40.3:5174/` (SAN `100.95.40.3`). | NONE on device. Fixture generator verified byte-identical across two runs. Launcher warned it cannot verify iPhone CA trust or reachability. | BLOCKED - no physical device; gate remains fully open |
| 2026-08-11 | DESKTOP-PWA-HOST-005-A | Back-end / Front-end | Corrected the advertised-address heuristic after it regressed the established Tailscale workflow. `collectLanAddresses` now classifies addresses (`overlay` for 100.64.0.0/10, `private`, `other`) and prefers mesh-VPN addresses, which reach an enrolled phone from any network without an inbound firewall rule for the physical adapter. The dashboard now lists every address with a Tailscale / Local network switch and per-kind reachability guidance, so the choice is explicit. | Host evidence: Ethernet adapter (192.168.100.41) is on the Public firewall profile with no inbound rule for 5174, while the iPhone is an enrolled tailnet node. Certificate SAN covers all addresses; receiver verified loading over `https://100.95.40.3:5174/` (title `DEQR Receive`, no horizontal overflow). Startup marker `preferred=overlay`. Desktop 20 files/215 tests PASS; PWA 7 files/25 tests PASS; typechecks, doctor, drift, `test:packaged`, diff-check, fuse verdict PASS. | PASS — corrected regression; physical iPhone validation still open under WEB-IOS-10 |
| 2026-08-11 | DESKTOP-PWA-HOST-005 | Back-end / Front-end / Security | Made the packaged desktop app publish the iPhone receiver itself, removing the Vite development-server dependency. Added `src/main/lan-addresses.ts` (advertisable IPv4 selection), `src/main/pwa-certificate.ts` (env / stored / generated TLS material persisted to userData so the phone trusts it once), `src/main/pwa-host.ts` (read-only GET/HEAD static HTTPS server with lexical containment and real security headers), and `src/renderer/components/PwaHostCard.tsx` (dashboard QR + URL). PWA build output moved to `dist/pwa` so the existing `dist/**/*` packaging rule ships it; `package`/`dist` now build it. | Live probe against the packaged app serving from inside `app.asar`: `/`, manifest, `sw.js`, PNG and SVG icons all 200 with correct types; SPA fallback 200; missing asset 404; `POST` 405 with `Allow`; encoded traversal 400. Markers `DEQR_PWA_HOST_READY port=5174 certificate=generated` then `certificate=stored` on re-run; 0 listeners after exit. Desktop 20 files/205 tests PASS; PWA 7 files/25 tests PASS; typechecks, doctor, drift, `test:packaged`, diff-check, fuse verdict PASS. | PASS (host side) — physical iPhone validation of the hosted origin remains open under WEB-IOS-10 |
| 2026-08-10 | WEB-IOS-PHYS-PKG-004 | Project Manager | Reconciled project control against the actual worktree, recorded before/after dirty-tree evidence fingerprints without committing, classified the residual CSP and privacy backlog items from evidence, and published the 20-section physical/packaged acceptance report. | Evidence fingerprints: pre-change patch SHA-256 `46C16542...A0235`, post-change `DE66BC06...B365C`; untracked manifest `F6E9867C...8D89A42`. Full gate matrix recorded in `.ai-team/reports/testing/WEB-IOS-PHYSICAL-PACKAGED-ACCEPTANCE-REPORT.md`. | NOT ACCEPTED - `WEB-IOS-10` unexecuted; no commit, push, or promotion performed |
| 2026-08-12 | TSK-028 | Front-end / UI-UX | Remediated desktop renderer accessibility on `codex/desktop-accessibility-remediation`: semantic landmarks with `aria-labelledby`, labelled window controls / QR surface / camera preview, blocking `alert()` replaced by inline `role="status"` and `role="alert"` messaging, camera acquisition gated behind an explicit Start Camera action, Escape-triggered `aria-modal` cancel confirmation, verified-completion screen, DPR-correct offscreen QR rasterization, and `frame: false` to remove the duplicated native header. Extracted `ui-model.ts` helpers with unit tests. | `npm run typecheck` (clean); `npm test` (131 passed / 14 files) | AUTOMATED PASS — MANUAL VISUAL & PACKAGED RETEST OPEN |
| 2026-08-12 | TSK-028.1 | Front-end / UI-UX | Fixed an accessibility regression introduced by `407a4b3` itself. `.action-card span` forced `--text-secondary` (`#94a3b8`) onto the primary gradient card, measuring 1.85:1 at `#00f2fe` and 1.06:1 at `#4facfe` against WCAG AA's 4.5:1; the description was effectively invisible at the gradient's blue end, and light mode was also marginal at 4.27:1. Scoped the colour override to non-primary cards so the primary card retains `button.primary`'s `#06121d` foreground, measuring 13.6:1 and 7.79:1. Found only by looking at a screenshot of a running window — typecheck and all 131 tests passed before and after. Branch then fast-forwarded into `main` (`5bc1586..bcc0527`) and pushed to `origin/main` at `adb3491`. | `npm run typecheck` (clean); `npm test` (131 passed / 14 files); `npm run doctor` (0 warnings); visual confirmation captured from a running Electron window | SUCCESS — CONTRAST RESTORED TO WCAG AA |
| 2026-08-16 | DESKTOP-REL-016 | Back-end / QA | Re-anchored the release artifacts after BRAND-001 invalidated them, and recorded the trap that caused it. Icon verification during BRAND-001 ran `npx electron-builder` directly, three times; each run wrote into `release/`, and `npm run release:verify` then reported all three recorded files drifted from the manifest — portable, NSIS and `app.asar`. The `a14411b` set could not be restored, so rather than leave the authoritative record describing files that no longer exist, a clean build was made from `95cd5bb` on `main` through `npm run release`, which is the only path that records what it built. New hashes: portable `deqr 0.1.0.exe` SHA-256 `46743F496D4E849C6CAD734AFD1B700BFCAFF03475EC58F28FF446C7AD4D17A5`; NSIS `deqr Setup 0.1.0.exe` SHA-256 `BF9BA738FA1969F909A4EEBF94FD555B55EC7DF738619F5C178BEA9758276789`; `app.asar` SHA-256 `4C96F6FD60679F862D03A0CF58145F530AA3E8D8AEB32639D2E3F9A93E2B6E9E`. The superseded hashes are retained in CURRENT-STATE marked unreachable, because acceptance evidence elsewhere in `.ai-team/` still cites them and a reader has to be able to tell those files are gone. This is the same class of artifact-staleness trap already recorded twice under WEB-IOS-10 and DESKTOP-REL-012, reached from the opposite direction: not a stale artifact left behind by `--dir`, but a current artifact written outside the recording path. | `npm run release` passed typecheck, tests, doctor and drift before building, then `verify-packaged-renderer` confirmed the shell assets resolve, the worker is `deqr-mobile-shell-v2` and network-first, and the main-process lifecycle guards are present. `npm run release:verify` re-run afterwards reports all three artifacts matching at commit `95cd5bb`. Icon resources extracted from both distributables and the unpacked exe: one icon group each of 16/24/32/48/64/128/256. | SUCCESS — MANIFEST RESTORED; FIRST RECORDED ARTIFACT CARRYING AN APPLICATION ICON |
| 2026-08-17 | WEB-IOS-SHELL-017 | PWA / Front-end / QA | Removed the mechanism that made a blank iPhone receiver permanent (`618dcb3`). A cached shell can name build assets the host no longer has; that alone is recoverable, and the service worker was making it terminal. `cacheFirst` answered a failed fetch with `new Response('Offline', { status: 503 })` including for module scripts — a synthetic response is still a response, so the browser MIME-checks it, rejects it, and fires nothing the page can observe — while `documentStrategy` falls back to the cached shell whenever its fetch throws, so the document never reaches the network again and every reload lands on the same dead build. With no error boundary and no asset-failure detection nothing could notice or escape, which is why the symptom survives redeploys. Recovery had to be a separate unhashed classic script: the failure *is* the hashed module not loading, so it cannot live inside it, and `script-src 'self'` without `unsafe-inline` rules out an inline script. `mobile-web/public/boot.js` listens for `error` on script and link elements — the only observable signal for a module that 404s — behind an 8s mount watchdog, purges only `deqr-` caches and DEQR's own workers, reloads exactly once behind a `sessionStorage` flag, and shows a diagnostic with the recorded boot stages rather than looping on a second failure; Private Browsing throwing on `sessionStorage` is treated as already-attempted so it cannot loop there either. The worker now returns `Response.error()` so a real network error reaches the element, treats a 404 on a hashed asset as a version mismatch by evicting the shell and posting `DEQR_SHELL_STALE` to clients, precaches `boot.js`, and no longer rejects the `PRECACHE_URLS` batch on one bad URL. React mounts first inside an error boundary that renders message-only detail, never a stack or payload. Investigated and disproved on the way: the host's SPA fallback is already restricted to extensionless paths, so a missing asset 404s rather than returning HTML under a JS URL. The camera path was not touched — nothing in it blocks the shell from mounting. | 4 new tests in `service-worker-strategy.test.ts` drive the real `sw.js`: the synthetic-response defect, stale-shell eviction plus client notification, the deliberate non-reaction to a missing non-build file, and `boot.js` precaching. PWA 63 PASS / 11 files (up 4); desktop 286 PASS / 25 files; both typechecks clean; both production builds PASS with `boot.js` emitted and referenced in the built HTML; `verify-packaged-renderer` PASS; doctor 0 warnings. **Not negative-proven** — the new tests were not run against the pre-fix worker to confirm they fail without it, a standard `DESKTOP-IPC-007` and `WEB-IOS-SCAN-014` both met. Physical iPhone Safari NOT EXECUTED. | PARTIALLY FIXED — PERMANENCE REMOVED; SAFARI-ONLY ROOT CAUSE UNPROVEN AND PHYSICAL VALIDATION PENDING |
| 2026-08-18 | DESKTOP-REL-018 | Back-end / QA | Rebuilt the distributables from `3358ac0` so a device test of the receiver blank page is possible at all. The recorded set predated WEB-IOS-SHELL-017, so it still shipped the pre-fix service worker and no `boot.js`; testing the reported symptom against it would have exercised the defect rather than the fix, which is the same trap logged under WEB-IOS-10 and DESKTOP-REL-012 in its stale-artifact form. New hashes: portable `deqr 0.1.0.exe` SHA-256 `3F092ADFC575455D4E80B686F91EE08235DE0D4F58D80FD2376B166D40A9B285`; NSIS `deqr Setup 0.1.0.exe` SHA-256 `E3CFA78D0D558EC5C96444E2D5C569B33DDB9042A6654411F13A9211E63DABCA`; `app.asar` SHA-256 `AF7C467DA1CB9DFC7EAB79E7DF432C89917E3694919C3FF080A642477D66CC30`. Note for future builds: Vite warns `<script src="./boot.js"> can't be bundled without type="module"`. That warning is the intended outcome, not a fault — the recovery script has to stay an unhashed classic script, so Vite copying it verbatim from `public/` is exactly what is wanted. | Verified inside `app.asar` by extraction rather than inferred from the build: `dist/pwa/boot.js` present at 5,557 bytes with its recovery flag intact, `index.html` referencing `./boot.js`, and `sw.js` as `deqr-mobile-shell-v3` precaching `boot.js`, returning `Response.error()` and posting `DEQR_SHELL_STALE`. A crude grep for the old synthetic `503 Offline` came back positive and was chased down rather than accepted: the only surviving instance is in `documentStrategy`, where a navigation with no network and no cached shell has no honest answer but an error. `npm run release` passed typecheck, tests, doctor and drift before building; `verify-packaged-renderer` confirmed shell assets, the network-first v3 worker and the main-process lifecycle guards. | SUCCESS — FIRST ARTIFACT CARRYING THE RECEIVER FIX; DEVICE TEST NOW MEANINGFUL |
| 2026-08-16 | BRAND-001 | Branding Designer / UI-UX / Front-end / Back-end / QA | Replaced the QR-block application icon with the DEQR Vortex identity across both applications (`9178b0c`, `95cd5bb`, fast-forwarded into `main`). Two defects were found in the audit before any design work: the existing mark was a literal QR finder pattern in a cyan (`#00f2fe`) that appears in no shipped interface surface — the live accent is `#0071e3` / `#2997ff` — and **the Windows applications had no icon at all**, because electron-builder carried no `win.icon`, so every packaged build, both installers, the taskbar and Alt+Tab showed the default Electron icon. The mark is five logarithmic-spiral arms converging on a focal emblem: radius decays geometrically along the sweep so it reads as turning without an arrow, the colour ramp is radial so the colour itself carries the convergence, and the emblem is the same vortex an order of magnitude down and inverted with a square module resolved at the centre. Four concepts were scored on a weighted matrix before selection, then refined across five study rounds judged on rendered pixels rather than intent; two negative results drove the final geometry — a disc-shaped focus reads as a pupil, and enlarging the emblem by pushing the arms outward destroys the vortex and yields a bullseye. Geometry is generated by `scripts/brand/build-masters.mjs` rather than hand-drawn, so the arms are exact rotations and the construction can be re-tuned; `branding/source/*.svg` are the masters and the only input to `scripts/brand/generate-brand-assets.mjs`, which produces every shipped raster. One mark, three size treatments, because the primary arms fall under two pixels around 32 px and the emblem blades speckle; the boundary lives in `tileFor()` alone so the brand sheet always shows what the applications ship. `deqr-chip.svg` exists for the one place a size switch is impossible — an `<img>` cannot vary artwork by rendered size, and the two headers, the receiver home mark and the SVG favicon all draw between 23 and 47 px. Two further corrections: the 192 and 512 manifest icons declared `purpose: "any maskable"`, which a platform mask crops to a circle so artwork drawn to fill the square loses its edges, and maskable is now its own inset asset; and the receiver had no `favicon.ico` fallback. A latent defect in the new tooling was found and fixed in `95cd5bb` — `build-masters.mjs` ran its CLI dispatch at module top level, so importing it for a geometry helper silently rewrote every master. No protocol, transfer, camera, IPC or security behaviour was touched. Specification in `branding/DEQR-BRAND-GUIDE.md`; design record in `branding/concepts/` and `branding/iterations/`. | Verified against artifacts rather than from the build succeeding. The generated `.ico` was decoded back out of the shipped file and every frame rendered — 16/24/32/48/64/128 as 32-bit DIBs and 256 as an embedded PNG, no malformed layer — and icon resources were extracted from `release/win-unpacked/deqr.exe`, the NSIS installer and the portable exe, all three carrying one icon group of 16/24/32/48/64/128/256. Small sizes were judged by magnifying the real 16/24/32/48 rasters with smoothing off, not by scaling the vector down; that is what exposed the need for the micro treatment. Maskable circle, squircle and rounded-square crops all preserve the mark, and the `any` tile was crop-tested alongside to show why the dedicated asset is required. The packaged app launched to `DEQR_RENDERER_READY dashboard=DEQR_OPTICAL_TRANSFER preload=available` and exited 0. The import-side-effect fix was verified both ways: importing now leaves every master's mtime untouched while still exposing all nine exports, and both CLI forms still write. Desktop 286 PASS / 25 files; PWA 59 PASS / 11 files, up 4 on a new `pwa-assets` contract asserting every advertised icon exists on disk and that exactly one dedicated maskable entry is declared; both typechecks, both production builds and `verify-packaged-renderer` PASS. | SUCCESS — DESKTOP AND PWA INTEGRATION VERIFIED AT ARTIFACT LEVEL; PHYSICAL HOME SCREEN ICON NOT EXECUTED |
| 2026-08-15 | WEB-IOS-SHELL-016 | Front-end / QA | Diagnosed a blank receiver page at `https://100.95.40.3:5174/` from a DevTools console showing 503s for `main.tsx`, `@react-refresh` and `client` — paths that exist only on a Vite development server, never in a production build. Root cause is an origin collision: `scripts/run-local.ps1` defaults `PwaPort` to 5174, the same origin the packaged receiver uses, and `mobile-web/src/main.tsx` registered the service worker unconditionally. Running the launcher therefore registered a worker for that origin and cached the **development** shell, whose `<script src="/src/main.tsx">` and injected `/@vite/*` URLs the packaged host cannot serve; the cache-first worker kept replaying it, and the worker's own offline fallback turned the failures into 503s. Server-side was verified healthy first and excluded: HTML, JS and CSS all 200 with correct MIME over both addresses, certificate SANs cover `100.95.40.3` and are valid to Sep 2027, and the shipped shell's asset references all resolve inside `app.asar`. Fixed by registering the worker only under `import.meta.env.PROD`, so development can never contaminate an origin production shares. A second root cause was found when the symptom persisted after the gate: the v2 worker could never take over. Its `install` used `cache.addAll(CORE)`, which is atomic — a single failed precache fetch rejects the batch, fails the install, and leaves the previous worker in charge permanently, including the very worker whose cached shell the update exists to replace. Over a self-signed origin an SW-initiated fetch failing is entirely plausible, and the 503s prove v1 was still controlling the page, since an activated v2 would have served the document network-first. Install now precaches each entry independently and tolerates failures: a worker that activates with an incomplete cache still goes to the network first and can fill in later, whereas one that never installs cannot do anything at all. | Production output is byte-identical — the bundle hash stayed `index-DE_uCX7f.js`, since `import.meta.env.PROD` is statically replaced — so shipping behaviour is unchanged and only development stops registering. Regression test in `tests/pwa-assets.test.ts` asserts both the gate and the launcher port collision it defends against, so removing the gate or changing the port surfaces here. PWA 57 PASS, desktop 286 PASS, typechecks clean, builds and `test:packaged` PASS, doctor 0 warnings, drift zero. | SUCCESS — DEV WORKER NO LONGER POISONS THE PRODUCTION ORIGIN |
| 2026-08-15 | DESKTOP-REL-015 | Back-end / QA | Rebuilt the distributables at HEAD `3e6b518` so a shippable artifact exists containing DESKTOP-CRASH-013 and WEB-IOS-SCAN-014. Every prior artifact — `16976EE1…`, `135A15FC…` and `80D42022…B66A` — ships the shutdown crash, because the packaging done during investigation predated both fixes. New hashes: portable `deqr 0.1.0.exe` SHA-256 `6DA0D07B06C997317E38432A9A9771CD226A88A8C8E296868D378A09D3B86921`; NSIS `6BA4DA589E82D0002B973E4B8E7389E0F450AD04C21C707A6E68EFF3B5BD1805`; `app.asar` `115871EB3035FFD6E5F2E04C9F684B42A813B020642720852869EF51159017BA`. | Verified by running the portable rather than inferring from build inputs: a real transfer was driven to 36 emitted frames through the native file dialog, and closing the window then exited in 2.4 s with 0 error dialogs, no `Object has been destroyed` in either stream, and 0 orphaned processes. Both fixes were additionally confirmed inside `app.asar` by extraction — `isRendererAlive`, `deliverToRenderer` and `disposeAll` in `dist/main`, `stalledRecoveries` and the "Scanner restarts" row in the PWA bundle, `deqr-mobile-shell-v2` plus `documentStrategy` in `sw.js`. | SUCCESS — FIRST SHIPPABLE ARTIFACT WITH BOTH FIXES |
| 2026-08-15 | DESKTOP-CRASH-013 | Back-end / QA | Fixed `TypeError: Object has been destroyed` thrown from `generateFrame` when the packaged app was closed during an active transfer. Root cause: the animated-QR interval is a Node timer with no relationship to the window that started it, so closing the window destroyed the renderer while the timer kept firing; `webContents.send` then threw inside a timer callback, where no caller exists to catch it, and Electron surfaced it as the main-process error dialog. Nothing removed the session on window close or on quit — `stopTransfer` ran only on explicit cancel — and neither `generateFrame` nor `loopbackFrame` checked whether the target was still alive. Reproduced first at source level with a WebContents double that throws on `send` exactly as the native binding does; the repro failed with the same error at the same function as the packaged stack trace, and the identical defect was confirmed in `loopbackFrame`. Fixed by adding `isRendererAlive` plus a `deliverToRenderer` helper that ends the session when the target is gone, adding `SessionManager.disposeAll`, and releasing sessions from `mainWindow.on('closed')`, `render-process-gone`, and `before-quit`. Exception containment is present only as a floor under a destruction landing between check and send; the lifecycle ownership is the fix. | Six regression tests in `tests/main/transfer-lifecycle.test.ts` covering destroy-during-transfer, timer cancellation, cancel-then-idle, session replacement, the loopback timer, and shutdown disposal. Packaged runtime matrix against `release/win-unpacked`, driving the real native file dialog by window message: normal-close, start-then-immediate-close, cancel-then-close, and loopback-then-close each exited in ~1.4 s with **0 error dialogs, no "Object has been destroyed" in stdout/stderr, and 0 orphaned processes**. Active-transfer close was exercised with 36 frames already emitted and the QR canvas live. Desktop suite 286 PASS. | SUCCESS — CRASH FIXED AND VERIFIED ON THE PACKAGED BUILD |
| 2026-08-15 | WEB-IOS-SCAN-014 | Front-end / QA | Fixed the iOS/PWA receiver scan failure. The failing boundary was **video readiness and scan-loop liveness**, not camera acquisition, QR decoding, or protocol: `schedule()` armed `requestVideoFrameCallback` alone, and rVFC only fires when the element actually presents a frame, so a camera that never starts presenting — or a stream that stalls across a lifecycle transition — left the loop permanently dead while the UI still reported "Camera active" and zero frames were ever decoded. A second stall existed because `schedule()` and `onVideoFrame()` both returned on `document.hidden` without arranging any future wake-up, so the loop could not resume. Fixed by pairing rVFC with a 500 ms watchdog timer whose first-to-fire cancels the other, by keeping a wake-up armed while backgrounded instead of returning empty-handed, and by catching a frame-level capture throw so one unreadable frame cannot end the session. Added a `stalledRecoveries` counter surfaced as "Scanner restarts" so a tester can separate a dead scanner from optics that will not decode. Protocol was already compatible and was not changed. | Five tests in `mobile-web/tests/camera-scan-loop.test.ts`; the watchdog test was verified to fail when the watchdog is removed. Reconstruction matrix extended to 5 KiB / 100 KiB / 500 KiB / 1 MiB with 10–20% deterministic frame loss through the real production modules, all byte-for-byte with matching SHA-256. **Real-browser end-to-end**: the built PWA was driven through a canvas-backed MediaStream replaying genuine desktop-emitted QR frames, reaching `File verified`, 100%, 7/7 unique blocks, 7 QR codes read, 0 duplicates, 0 foreign. Decisive evidence for the root cause: `Scanner restarts 7` — rVFC fired zero times and **every** scan came from the watchdog, so the pre-fix build would have received nothing. PWA suite 56 PASS. Physical iPhone remains unexecuted. | SUCCESS — SCANNER STALL FIXED; PHYSICAL GATE STILL PENDING |
| 2026-08-14 | DESKTOP-REL-012 | Back-end / QA | Rebuilt the distributable artifacts at HEAD `b669246` with `npm run dist`, because the previous portable was cut at 02:59 and predated the receiver-control focus fix committed at 17:28 — the same `--dir` staleness trap recorded under WEB-IOS-10, hit a second time within one day. New hashes: portable `deqr 0.1.0.exe` SHA-256 `16976EE1A2042E4DB425E0776A212F69D371D41015539900B3D918EAA3DE9E9D`; NSIS `deqr Setup 0.1.0.exe` SHA-256 `51FB940E79B6A88EADED1AA956EF4D55C2092B1904BAB9A689B1C3C2545BADBF`; `app.asar` SHA-256 `870D6BFB0999D913B5F86D919F89B75B522BF790555A8ADE45F5EBAFF5E56931`. | Verified by running the portable itself rather than inferring from the build inputs: pressing Start by real keyboard input ends on `activeEl: THE BUTTON`, so the shipped artifact carries the focus fix; the receiver bound `0.0.0.0:5174` and answered `GET /health` with `200 application/json`, `Cache-Control: no-store` and the CSP header over real HTTPS; Stop traced `Stopping… (disabled, BODY) -> Start receiver (enabled, BODY) -> BUTTON` and released the port. The packaged PWA remains the fixed shell (`deqr-mobile-shell-v2`, `documentStrategy`, `skipWaiting`, health bypass). Zero stray processes; ports 5174 and 9222 released. | SUCCESS — DISTRIBUTABLES CURRENT AT b669246 |
| 2026-08-14 | DESKTOP-UI-011 | QA Engineer / Front-end / UI-UX | Closed the receiver-control half of the desktop interaction gate using the DESKTOP-UI-010 technique, and fixed the defect it exposed. Start and Stop were pressed by real keyboard `Enter` against `release/win-unpacked/deqr.exe`: the receiver went `stopped -> running`, bound `0.0.0.0:5174`, answered `GET /health` with `200 application/json`, `Cache-Control: no-store` and the CSP header over real HTTPS, then released the port on stop. `aria-busy` toggled `false -> true -> false`, the live region tracked each state, and the control was not remounted (`sameNode` stayed true), so the single-button design in `PwaHostCard` does what its comment claims. **The gate failed on first run**: keyboard focus was dropped to `<body>` and never returned. The mechanism was isolated rather than inferred — focusing the control and setting `disabled = true` yields `stillFocusedWhenDisabled: false` and `focusLandedOn: BODY`, and re-enabling yields `refocusedOnReenable: false` — so `actionDisabled` defeated the stated intent of the non-remounting design, leaving a keyboard user at the top of the document after pressing Enter, having to tab back to reach the button they had just used. Fixed with `shouldRestoreActionFocus` in `pwa-host-model.ts`, applied from an effect that runs when the control is re-enabled and reclaims focus only when the control had it, the transition has settled, and focus is on `<body>`; focus deliberately moved elsewhere during a start is left alone. The predicate lives in the model rather than the component because the renderer suite runs in a node environment with no component-testing harness, so this keeps it unit-testable without introducing one. | Before the fix: Start ended `activeEl: BODY (focus lost)`; the Stop trace was `Stop receiver (enabled, BODY) -> Stopping… (disabled, BODY) -> Start receiver (enabled, BODY)`. After rebuilding and repackaging: Start ends `activeEl: THE BUTTON`, and the Stop trace ends `Stopping… (disabled, BODY) -> Start receiver (enabled, BODY) -> BUTTON`. Focus necessarily leaves during the disabled phase because a disabled element cannot hold it; returning it once the control is usable is the achievable behaviour. Full gates from the fixed tree: `npm run typecheck` clean; `npm test` 280 tests PASS (up from 278); `npm run build` and `npm run package` PASS; `npm run test:packaged` PASS; doctor 0 warnings; drift zero. Ports 5174 and 9222 released and zero stray processes after every run. | PASS — DEFECT FOUND AND FIXED; SCREEN-READER ANNOUNCEMENT STILL UNVERIFIED |
| 2026-08-14 | DESKTOP-UI-010 | QA Engineer / Front-end | Verified the merged desktop window chrome — the `frame: false` plus custom-title-bar combination left by `b4eb147` — against `release/win-unpacked/deqr.exe` rather than the development shell. Session state was checked first rather than assumed: `LogonUI` is running, but for the disconnected console session (id 3); the working session (id 1, `rdp-tcp#2`) is Active and interactive. `CopyFromScreen` still failed with an invalid handle because the shell is not attached to the interactive window station, so capture used `PrintWindow` with `PW_RENDERFULLCONTENT` and interaction used CDP against a `--remote-debugging-port` launch. Findings: exactly one header, with the native frame contributing zero pixels since the restored client rect equals the window rect — `WS_CAPTION` and `WS_THICKFRAME` remain set, which is how Electron retains snap and resize, so the style bits alone would have been misleading. Drag region probed with `WM_NCHITTEST`: title-bar empty area and brand return `HTCAPTION`, all three controls and the page body return `HTCLIENT`, exactly the `drag`/`no-drag` split intended. Minimize, maximize/restore and close driven by real `Input.dispatchMouseEvent` clicks on the custom buttons, exercising renderer to preload to `handleTrusted` IPC to `BrowserWindow`. Maximized, the window rect overhangs the work area by 7 px per edge but the client rect is exactly `1440x860`, so the overhang is invisible resize border and the title bar is not clipped. Real `Tab` traverses Minimize, Maximize/restore, Close with the `--focus-ring` box-shadow applied. Three measurement errors were caught and corrected rather than reported as defects: a DPI-scaled `PrintWindow` capture that cropped the controls out of frame, hit-test coordinates sent from a DPI-unaware process, and a probe run against an already-minimized window. | Evidence: `headerCount: 1`; restored `GetClientRect` == `GetWindowRect` 1024x768; maximized window `-7,-7 1453x873` vs client `1440x860` vs work area `1440x860`; hit tests `HTCAPTION`/`HTCAPTION`/`HTCLIENT`/`HTCLIENT`/`HTCLIENT`/`HTCLIENT`; minimize click -> `IsIconic` true; maximize click -> `IsZoomed` true, second click -> false; close click -> process exited in 0 s, zero stray processes, ports 5173/5174/9222 all released; focus `box-shadow` `rgba(0, 113, 227, 0.34) 0 0 0 3px` matching the `--focus-ring` token; controls 46x44 px. | PASS — with two stated caveats: the interaction run was debug-enabled and therefore not the shipping launch configuration, and screen-reader announcement was not tested |
| 2026-08-14 | WEB-IOS-10 | QA Engineer | Physical iPhone verification of the packaged build was requested and **was not executed**: no device is available to the session, and `tailscale status` additionally shows `iphone-13-mini` (100.97.189.101) offline, last seen 2 days ago, so the target was not even reachable. Prepared the gate instead and caught one hazard that would have invalidated the run: `release/deqr 0.1.0.exe` was dated 2026-08-07 while `release/win-unpacked/` was current, because `npm run package` uses `--dir` and never rebuilds the portable. Anyone reaching for the obvious single-file artifact would have tested a build predating the ios2-shell merge, the receiver start/stop lifecycle, the PWA stale-shell fix, and the IPC sender policy. Rebuilt with `npm run dist` and re-verified the host side up to the device boundary. | Packaged receiver confirmed inside `app.asar` by extraction, not inference: `sw.js` carries `deqr-mobile-shell-v2`, `documentStrategy`, `skipWaiting` and the `/health` bypass, and `index.html` references `assets/index-CA3BgLcw.js`, matching the 2026-08-14 PWA build. New artifacts: portable `deqr 0.1.0.exe` SHA-256 `135A15FC240B8867E63070C575DECC24C41E181BC2724B5D8D928B1A332DF1A8`; NSIS `deqr Setup 0.1.0.exe` SHA-256 `6F4DA9A3C1B64C8E67EFD88783EC2483691743BB41D56C939CD42396319187E8`; `app.asar` SHA-256 `FED5A9609C2A10AE69A36C221CE622D08EDC8363AB1CB943B484466C188B6490`. Portable readiness proven by control experiment, since a portable wrapper does not surface child stdout: with autoclose enabled it self-exited after 5.6 s, and with autoclose disabled it was still running at 20 s, so the exit was renderer readiness rather than a crash. `win-unpacked` binary launched clean with `DEQR_RENDERER_READY` and empty stderr. No listener on 5174 after any run. Advertised addresses: `https://100.95.40.3:5174/` (overlay) and `https://192.168.100.41:5174/` (private). | NOT EXECUTED — NO PHYSICAL DEVICE; GATE REMAINS FULLY OPEN |
| 2026-08-14 | DESKTOP-UI-009 | Front-end / System Architect / QA | Resolved the orphaned `src/renderer/ui-model.ts`. Confirmed the recorded history rather than assuming it: `git log --follow` shows `407a4b3` created the module, `git log -S "from './ui-model'"` shows the same commit imported it from `App.tsx`, and `git show 407a4b3:src/renderer/App.tsx` still contains that import plus an "Estimated lower bound" row; the `b4eb147` merge then resolved `App.tsx` to the `ios2-shell` version, which carries its own private helpers and renders no estimate. Compared each export against shipping code rather than stopping at the import graph. `getQrRasterSize` served a DPR rasterization approach that no longer exists — the merged `QRCanvas` paints a fixed 400px canvas. `estimateMinimumStreamSeconds` is rendered nowhere and is also stale, defaulting to 30 FPS while the sender ships `DEFAULT_OPTICAL_TRANSFER_FPS = 10`. `formatFileSize` and `getIpcErrorMessage` were duplicated inline in `App.tsx`, but the shipping copies are better: `App.tsx` labels KiB/MiB where the orphan labelled KB/MB over a 1024 divisor, and its error helper is typed and falls back to a message instead of returning undefined. Reinstating the module would therefore have imported a user-visible unit regression and revived two dead functions, so it was deleted with its isolated test. To avoid losing the coverage that still mattered, `formatFileSize` and `getIpcError` moved from private functions in `App.tsx` into `app-model.ts` — the renderer domain module `App.tsx` already imported — and are now covered in `tests/renderer/app-model.test.ts`, including the no-message error case that returns a usable fallback. | Focused: `tests/renderer` 7 files / 27 tests PASS, with `app-model` growing from 3 to 6 tests as `ui-model`'s 4 isolated tests were retired. Repository-wide search confirms no remaining `ui-model` reference outside one explanatory comment and the historical TSK-028 log entry. Full: `npm run typecheck` clean; `npm test` 24 files / 278 tests PASS (279 before, less `ui-model`'s 4 isolated tests, plus 3 at the production layer); `npm run build` PASS; `npm run doctor` PASSED (0 warnings); `npm run drift-check` zero drift. | SUCCESS — ORPHAN REMOVED, LIVE BEHAVIOUR CENTRALIZED AND COVERED |
| 2026-08-14 | DESKTOP-SEC-008 | Cybersecurity / System Architect / Back-end / QA | Verified that no renderer-to-main IPC handler validated its sender, and that the surrounding controls claimed by the review are all real in current source: `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true`, packaged CSP `default-src 'none'` with `connect-src 'none'` and `frame-src 'none'`, `will-navigate` denied outright, popups denied, and fail-closed request filtering. Cybersecurity assessment concluded that context isolation protects the bridge from tampering but says nothing about who called it, so `window.deqr` reached a file dialog, a file write, the transfer/loopback engines, the window controls, and `pwaHost:start` — which mints a 2048-bit key and binds every interface — from any script in the renderer context. Recorded as defence in depth rather than remediation of a live exploit, and adopted because privileged IPC is a capability boundary and the cost is near zero. Added `src/main/ipc-sender-policy.ts` and routed all 15 channels through a `handleTrusted` wrapper so the guard lives in registration rather than in each body. Two traps were avoided rather than inherited: `isTrustedRendererOrigin` also accepts `data:` and `devtools:`, which may load but must never call, and it takes no `isPackaged`, so reusing it would have trusted `http://localhost:5173` inside a shipped app. Extracted `isLocalRendererFileUrl` so "the packaged renderer document" has one definition consumed by both policies, and fixed a latent hole in `isAllowedLocalRendererResource` where a host-bearing `file:` URL fell through to the permissive protocol check. Subframes are rejected, and a frame that does not report `parent` is rejected rather than assumed top-level. Window controls were considered for an exemption and deliberately not given one. See ADR-010. | Enumeration test drives the real `registerIpcHandlers` and requires every registered channel to reject an untrusted sender; verified by temporarily registering a raw `ipcMain.handle`, which failed the suite naming `temp:bypassProbe`, then passed once removed. Also covers packaged-vs-development trust in both directions, the PWA origin on 5174, wrong scheme/port/host, credentials, suffix confusion, `data:`/`devtools:`, malformed and non-string values, subframes, destroyed and throwing frames, sanitized error text, and a spy proving `pwaHostLifecycle.start` is never reached after rejection. Focused: `ipc-sender-policy`, `ipc-contract`, `permissions`, `network-policy`, `ipc`, `receive-handler` — 6 files / 59 tests PASS. Full: `npm run typecheck` clean; `npm test` 25 files / 279 tests PASS (up from 24/264); `npm run doctor` PASSED (0 warnings); `npm run drift-check` zero drift. | SUCCESS — ALL 15 CHANNELS AUTHENTICATED, FAIL-CLOSED |
| 2026-08-14 | DESKTOP-IPC-007 | System Architect / Back-end / Front-end / Security / QA | Verified and resolved a preload/main IPC contract mismatch. `src/preload/index.ts` exposed `loopback.saveVerifiedResult` invoking `loopback:saveVerifiedResult`, and `DeqrAPI` declared it, but `registerIpcHandlers` never registered a handler, so any call would have rejected with "No handler registered". Removed the method and its type rather than implementing it. Evidence: `git log -S "loopback.saveVerifiedResult" --all` returns no commit on any branch that ever called it — it was speculative surface from the original M1 vertical slice `0bc4502`; `loopbackFrame` calls `globalSessionManager.removeSession` the moment decoding completes, zeroing `session.payload`, and the reconstructed buffer is a function local, so nothing survives for a later save to read; loopback re-decodes a file the user selected from local disk, so persisting it would duplicate a file that already exists; and `LoopbackView` already tells the user "No file is saved from this result." Implementing it would have required retaining payloads past completion, contradicting the deliberate wipe, and would have created a second file-save path beside `receive:saveReceivedFile`. Security position: this removes a preload-exposed file-save capability rather than adding one, so the change strictly reduces attack surface and introduces no new filesystem reachability. Added `tests/main/ipc-contract.test.ts`, which walks the real exposed bridge to collect every invoke channel and executes the real `registerIpcHandlers` to collect every handled channel, then asserts the first set is contained in the second; main-to-renderer push channels are asserted to be subscriptions rather than invoke contracts, so the rule stays correct rather than merely strict. | Guard proven by reintroducing the defect: the suite failed naming `loopback:saveVerifiedResult` as orphaned, then passed once reverted. Focused: `tests/main/ipc-contract.test.ts`, `tests/preload/*`, `tests/main/ipc.test.ts`, `tests/main/receive-handler.test.ts` — 5 files / 27 tests PASS. Full: `npm run typecheck` clean; `npm test` 24 files / 264 tests PASS (up from 23/260); `npm run doctor` PASSED (0 warnings); `npm run drift-check` zero drift. | SUCCESS — DEAD API SURFACE REMOVED, DRIFT NOW GUARDED |
| 2026-08-13 | WEB-IOS-PWA-011 | Front-end / Back-end / Security / QA / UI-UX | Investigated a user report that the iPhone web app received no data, appeared available whether or not the receiver was running, showed no service indicator, and rendered "Return to home" out of line. Established first that the receive path itself is not at fault: a new compositional test drives real desktop frames through real painted QR codes and jsQR into `ReceiverSession` to a verified SHA-256, and the shipped decoder worker chunk was confirmed in a live browser to return a 532-byte frame byte-exact. The defect that can strand a phone is the service worker: it answered every same-origin GET from a fixed `deqr-mobile-shell-v1` cache, including the HTML document, with no `skipWaiting` and no update path, so a phone that installed before `0e3e6dc` could never be given the corrected receiver — and the host's `Cache-Control: no-cache` on the shell was silently defeated. Rewrote it as network-first for documents with a cached offline fallback, cache-first only for content-hashed `/assets/`, background revalidation elsewhere, `deqr-mobile-shell-v2`, immediate claim, and the health path excluded so reachability is measured rather than remembered. Added a constant, `no-store`, `HEAD`-safe `GET /health` to the desktop host, matched before the single-page fallback that would otherwise answer it with the HTML shell; it publishes no address, interface, certificate, session, or file detail. Added `mobile-web/src/host-status.ts` and a topbar chip that polls only while visible, verifies the host's own marker rather than trusting a 200, bounds each probe with `AbortController`, backs off differently when reachable, and publishes only transitions so its polite live region never repeats. Replaced the static "Receiver" chip, which only restated the app's name. `RawQrDecoder` now reports worker failure instead of silently resolving empty forever, and a wedged `postMessage` can no longer strand every later frame; the UI separates "Scanner unavailable" from "Camera unavailable". Scan details gained QR-decode and foreign-session counts, both counts only. `.action-dock .primary ~ button` now spans the dock, and the home screen sizes by flex rather than an assumed header height. Set the PWA `worker.format: 'es'` so the emitted chunk matches its `{ type: 'module' }` constructor. | Desktop 23 files/260 tests PASS (up from 252); PWA 10 files/47 tests PASS (up from 25/7); both typechecks, `npm run build`, `npm run mobile-web:build`, doctor (0 warnings), drift-check, and `git diff --check` PASS. Live browser on the real built bundle served through the real desktop handler: worker decoded a real 532-byte desktop frame exact=true; measured at 390x844 the primary and "Return to home" are both 310px wide at the same left edge, height, radius and type, while the danger/secondary pair stays a two-column row; zero horizontal overflow and zero vertical overflow in both host states. Stopping the host flipped the chip to "Receiver unavailable" with recovery guidance while the page stayed rendered, and restarting it returned to "Receiver online" without a reload. Real host lifecycle over HTTPS on 5174: no listener and health unreachable before Start; after Start health returned 200 `{"service":"deqr-pwa-host","status":"ok"}` with `no-store` and the receiver CSP, the shell returned `no-cache`; after Stop the listener and health both went away; restart succeeded on `certificate=stored`. | AUTOMATED + COMPOSITIONAL + LOCAL-RUNTIME PASS — PHYSICAL iPHONE RE-SCAN NOT EXECUTED |
| 2026-08-13 | DESKTOP-PWA-HOST-006 | Back-end / Front-end | Put the iPhone receiver behind an explicit Start/Stop control and made it off at launch (`831ddb8`, pushed to `origin/main`). Previously it published itself on every run, binding `0.0.0.0:5174` and generating a 2048-bit private key before anyone had asked for the feature — and it usually lost the port race to `run-local.ps1` and failed silently anyway. Off by default is now the absence of a startup call rather than a flag, guarded by a source-text assertion. Added `src/main/pwa-host-lifecycle.ts` to own the server handle and serialize start/stop, where stop chains and start coalesces so a race always settles stopped; a `stopped/starting/running/stopping/failed` discriminator with five status factories that keep `running` and `state` in agreement; a listener fan-out feeding the new app-scoped `pwaHost:status` broadcast alongside `pwaHost:start` and `pwaHost:stop`; and a durable post-listen `'error'` handler, since the previous code removed its only one and an unhandled emitter error takes down the main process. Rewrote `PwaHostCard` around one derived button and a persistent live region, so keyboard focus is not dropped mid-action and a stopped receiver is no longer drawn as a failure. | `npm run typecheck` (clean); `npm test` (252 passed / 23 files, up from 220/21); `npm run build`; `npm run doctor` (0 warnings); `npm run drift-check`; `npm run mobile-web:test` (25 passed). Launched the app and confirmed no `DEQR_PWA_HOST_*` marker and no listener on 5174. Drove the real lifecycle outside the tests, which all fake the listen: certificate generated, 5174 bound, closed, then restarted onto `certificate=stored`, with socket checks either side. | SUCCESS (AUTOMATED + REAL-PATH) — CLICK-THROUGH AND SCREEN-READER PASS STILL OPEN |
