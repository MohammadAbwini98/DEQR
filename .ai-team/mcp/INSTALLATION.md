# MCP Installation & Environment Guide

## Usage Instructions
1. Copy `mcp.example.json` to your local environment configuration directory.
2. Replace environment placeholders (`${CODEBASE_MEMORY_MCP_PATH}`, `${WORKSPACE_ROOT}`) with your machine-specific paths.
3. Do **NOT** commit your local configuration containing personal absolute paths or keys to Git.
